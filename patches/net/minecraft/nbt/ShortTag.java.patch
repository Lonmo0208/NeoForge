--- a/net/minecraft/nbt/ShortTag.java
+++ b/net/minecraft/nbt/ShortTag.java
@@ -77,11 +_,7 @@
 
     @Override
     public boolean equals(Object p_129265_) {
-        if (this == p_129265_) {
-            return true;
-        } else {
-            return p_129265_ instanceof ShortTag && this.data == ((ShortTag)p_129265_).data;
-        }
+        return this == p_129265_ ? true : p_129265_ instanceof ShortTag && this.data == ((ShortTag)p_129265_).data;
     }
 
     @Override
@@ -143,7 +_,7 @@
         }
 
         static {
-            for(int i = 0; i < cache.length; ++i) {
+            for (int i = 0; i < cache.length; i++) {
                 cache[i] = new ShortTag((short)(-128 + i));
             }
         }
