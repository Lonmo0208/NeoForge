--- a/net/minecraft/server/level/ServerBossEvent.java
+++ b/net/minecraft/server/level/ServerBossEvent.java
@@ -11,20 +_,14 @@
 import net.minecraft.network.protocol.game.ClientboundBossEventPacket;
 import net.minecraft.util.Mth;
 import net.minecraft.world.BossEvent;
-import net.minecraft.world.entity.Entity;
-import net.minecraft.world.phys.Vec3;
 
 public class ServerBossEvent extends BossEvent {
     private final Set<ServerPlayer> players = Sets.newHashSet();
     private final Set<ServerPlayer> unmodifiablePlayers = Collections.unmodifiableSet(this.players);
     private boolean visible = true;
 
-    public ServerBossEvent(Entity p_342532_, BossEvent.BossBarColor p_340339_, BossEvent.BossBarOverlay p_341949_) {
-        super(p_342532_.getUUID(), p_342532_.getDisplayName(), p_340339_, p_341949_, p_342532_.position(), -1);
-    }
-
     public ServerBossEvent(Component p_8300_, BossEvent.BossBarColor p_8301_, BossEvent.BossBarOverlay p_8302_) {
-        super(Mth.createInsecureUUID(), p_8300_, p_8301_, p_8302_, Vec3.ZERO, 0);
+        super(Mth.createInsecureUUID(), p_8300_, p_8301_, p_8302_);
     }
 
     @Override
@@ -82,14 +_,6 @@
     }
 
     @Override
-    public void setLocation(Vec3 p_342040_, int p_341520_) {
-        if (p_342040_.distanceToSqr(this.center) > 1.0 || p_341520_ != this.radius) {
-            super.setLocation(p_342040_, p_341520_);
-            this.broadcast(ClientboundBossEventPacket::createUpdatePositionPacket);
-        }
-    }
-
-    @Override
     public void setName(Component p_8311_) {
         if (!Objects.equal(p_8311_, this.name)) {
             super.setName(p_8311_);
@@ -101,7 +_,7 @@
         if (this.visible) {
             ClientboundBossEventPacket clientboundbosseventpacket = p_143225_.apply(this);
 
-            for(ServerPlayer serverplayer : this.players) {
+            for (ServerPlayer serverplayer : this.players) {
                 serverplayer.connection.send(clientboundbosseventpacket);
             }
         }
@@ -121,7 +_,7 @@
 
     public void removeAllPlayers() {
         if (!this.players.isEmpty()) {
-            for(ServerPlayer serverplayer : Lists.newArrayList(this.players)) {
+            for (ServerPlayer serverplayer : Lists.newArrayList(this.players)) {
                 this.removePlayer(serverplayer);
             }
         }
@@ -135,7 +_,7 @@
         if (p_8322_ != this.visible) {
             this.visible = p_8322_;
 
-            for(ServerPlayer serverplayer : this.players) {
+            for (ServerPlayer serverplayer : this.players) {
                 serverplayer.connection
                     .send(p_8322_ ? ClientboundBossEventPacket.createAddPacket(this) : ClientboundBossEventPacket.createRemovePacket(this.getId()));
             }
