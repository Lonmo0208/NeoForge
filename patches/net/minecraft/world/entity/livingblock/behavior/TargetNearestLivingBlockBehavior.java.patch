--- a/net/minecraft/world/entity/livingblock/behavior/TargetNearestLivingBlockBehavior.java
+++ b/net/minecraft/world/entity/livingblock/behavior/TargetNearestLivingBlockBehavior.java
@@ -1,8 +_,8 @@
 package net.minecraft.world.entity.livingblock.behavior;
 
+import java.util.Comparator;
 import java.util.function.Predicate;
 import net.minecraft.server.level.ServerLevel;
-import net.minecraft.world.entity.ai.targeting.TargetingConditions;
 import net.minecraft.world.entity.livingblock.LivingBlock;
 import net.minecraft.world.entity.livingblock.Target;
 import net.minecraft.world.entity.livingblock.cognition.Intent;
@@ -33,15 +_,11 @@
     @Override
     public boolean tick(LivingBlock entity, ServerLevel level, int tickCount) {
         Vec3 pos = entity.position();
-        LivingBlock nearestOfType = level.getNearestEntity(
-            LivingBlock.class,
-            TargetingConditions.livingBlock().selector((e, var2) -> e instanceof LivingBlock lb && this.predicate.test(lb)),
-            null,
-            pos.x(),
-            pos.y(),
-            pos.z(),
-            AABB.around(pos, 20.0)
-        );
+        AABB area = AABB.around(pos, 20.0);
+        LivingBlock nearestOfType = level.getEntitiesOfClass(LivingBlock.class, area, this.predicate)
+            .stream()
+            .min(Comparator.comparingDouble(e -> e.distanceToSqr(entity)))
+            .orElse(null);
         this.moveTowards.update(Target.nearEntity(nearestOfType, 1.0));
         this.lastTargetedTick = tickCount;
         return false;
