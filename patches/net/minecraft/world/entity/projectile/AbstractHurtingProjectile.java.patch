--- a/net/minecraft/world/entity/projectile/AbstractHurtingProjectile.java
+++ b/net/minecraft/world/entity/projectile/AbstractHurtingProjectile.java
@@ -19,6 +_,7 @@
 import net.minecraft.world.phys.Vec3;
 
 public abstract class AbstractHurtingProjectile extends Projectile {
+    public static final double DEFLECTION_SCALE = 0.05;
     public double xPower;
     public double yPower;
     public double zPower;
@@ -87,8 +_,8 @@
             }
 
             HitResult hitresult = ProjectileUtil.getHitResultOnMoveVector(this, this::canHitEntity, this.getClipType());
-            if (hitresult.getType() != HitResult.Type.MISS) {
-                this.onHit(hitresult);
+            if (hitresult.getType() != HitResult.Type.MISS && !net.neoforged.neoforge.event.EventHooks.onProjectileImpact(this, hitresult)) {
+                this.hitOrDeflect(hitresult);
             }
 
             this.checkInsideBlocks();
@@ -99,7 +_,7 @@
             ProjectileUtil.rotateTowardsMovement(this, 0.2F);
             float f;
             if (this.isInWater()) {
-                for(int i = 0; i < 4; ++i) {
+                for (int i = 0; i < 4; i++) {
                     float f1 = 0.25F;
                     this.level().addParticle(ParticleTypes.BUBBLE, d0 - vec3.x * 0.25, d1 - vec3.y * 0.25, d2 - vec3.z * 0.25, vec3.x, vec3.y, vec3.z);
                 }
@@ -122,12 +_,6 @@
     }
 
     @Override
-    public void lerpMotion(double p_333799_, double p_334051_, double p_333762_) {
-        super.lerpMotion(p_333799_, p_334051_, p_333762_);
-        this.assignPower(p_333799_, p_334051_, p_333762_);
-    }
-
-    @Override
     protected boolean canHitEntity(Entity p_36842_) {
         return super.canHitEntity(p_36842_) && !p_36842_.noPhysics;
     }
@@ -242,5 +_,12 @@
             this.yPower = p_334030_ / d0 * 0.1;
             this.zPower = p_333897_ / d0 * 0.1;
         }
+    }
+
+    @Override
+    public void onDeflection() {
+        this.xPower = this.getDeltaMovement().x * 0.05;
+        this.yPower = this.getDeltaMovement().y * 0.05;
+        this.zPower = this.getDeltaMovement().z * 0.05;
     }
 }
