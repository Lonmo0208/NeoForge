--- a/net/minecraft/world/entity/monster/Ghast.java
+++ b/net/minecraft/world/entity/monster/Ghast.java
@@ -47,7 +_,7 @@
         this.goalSelector.addGoal(7, new Ghast.GhastLookGoal(this));
         this.goalSelector.addGoal(7, new Ghast.GhastShootFireballGoal(this));
         this.targetSelector
-            .addGoal(1, new NearestAttackableTargetGoal<>(this, Player.class, 10, true, false, p_340131_ -> Math.abs(p_340131_.getY() - this.getY()) <= 4.0));
+            .addGoal(1, new NearestAttackableTargetGoal<>(this, Player.class, 10, true, false, p_337849_ -> Math.abs(p_337849_.getY() - this.getY()) <= 4.0));
     }
 
     public boolean isCharging() {
@@ -199,7 +_,7 @@
         public void tick() {
             if (this.operation == MoveControl.Operation.MOVE_TO) {
                 if (this.floatDuration-- <= 0) {
-                    this.floatDuration += this.ghast.getRandom().nextInt(5) + 2;
+                    this.floatDuration = this.floatDuration + this.ghast.getRandom().nextInt(5) + 2;
                     Vec3 vec3 = new Vec3(this.wantedX - this.ghast.getX(), this.wantedY - this.ghast.getY(), this.wantedZ - this.ghast.getZ());
                     double d0 = vec3.length();
                     vec3 = vec3.normalize();
@@ -215,7 +_,7 @@
         private boolean canReach(Vec3 p_32771_, int p_32772_) {
             AABB aabb = this.ghast.getBoundingBox();
 
-            for(int i = 1; i < p_32772_; ++i) {
+            for (int i = 1; i < p_32772_; i++) {
                 aabb = aabb.move(p_32771_);
                 if (!this.ghast.level().noCollision(this.ghast, aabb)) {
                     return false;
@@ -261,7 +_,7 @@
                 double d0 = 64.0;
                 if (livingentity.distanceToSqr(this.ghast) < 4096.0 && this.ghast.hasLineOfSight(livingentity)) {
                     Level level = this.ghast.level();
-                    ++this.chargeTime;
+                    this.chargeTime++;
                     if (this.chargeTime == 10 && !this.ghast.isSilent()) {
                         level.levelEvent(null, 1015, this.ghast.blockPosition(), 0);
                     }
@@ -282,7 +_,7 @@
                         this.chargeTime = -40;
                     }
                 } else if (this.chargeTime > 0) {
-                    --this.chargeTime;
+                    this.chargeTime--;
                 }
 
                 this.ghast.setCharging(this.chargeTime > 10);
