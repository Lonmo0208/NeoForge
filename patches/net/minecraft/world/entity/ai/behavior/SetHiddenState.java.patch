--- a/net/minecraft/world/entity/ai/behavior/SetHiddenState.java
+++ b/net/minecraft/world/entity/ai/behavior/SetHiddenState.java
@@ -1,7 +_,6 @@
 package net.minecraft.world.entity.ai.behavior;
 
 import net.minecraft.core.BlockPos;
-import net.minecraft.core.GlobalPos;
 import net.minecraft.server.level.ServerLevel;
 import net.minecraft.world.entity.LivingEntity;
 import net.minecraft.world.entity.ai.behavior.declarative.BehaviorBuilder;
@@ -16,15 +_,13 @@
         int i = p_259244_ * 20;
         MutableInt mutableint = new MutableInt(0);
         return BehaviorBuilder.create(
-            p_259055_ -> p_259055_.group(
-                        p_259055_.present(MemoryModuleType.HIDING_PLACE), p_259055_.present(MemoryModuleType.HEARD_BELL_TIME)
-                    )
-                    .apply(p_259055_, (p_260296_, p_260145_) -> (p_340029_, p_340030_, p_340031_) -> {
+            p_259055_ -> p_259055_.group(p_259055_.present(MemoryModuleType.HIDING_PLACE), p_259055_.present(MemoryModuleType.HEARD_BELL_TIME))
+                    .apply(p_259055_, (p_260296_, p_260145_) -> (p_330034_, p_330035_, p_330036_) -> {
                             long j = p_259055_.<Long>get(p_260145_);
-                            boolean flag = j + 300L <= p_340031_;
+                            boolean flag = j + 300L <= p_330036_;
                             if (mutableint.getValue() <= i && !flag) {
-                                BlockPos blockpos = p_259055_.<GlobalPos>get(p_260296_).pos();
-                                if (blockpos.closerThan(p_340030_.blockPosition(), (double)p_260263_)) {
+                                BlockPos blockpos = p_259055_.get(p_260296_).pos();
+                                if (blockpos.closerThan(p_330035_.blockPosition(), (double)p_260263_)) {
                                     mutableint.increment();
                                 }
         
@@ -32,7 +_,7 @@
                             } else {
                                 p_260145_.erase();
                                 p_260296_.erase();
-                                p_340030_.getBrain().updateActivityFromSchedule(p_340029_.getDayTime(), p_340029_.getGameTime());
+                                p_330035_.getBrain().updateActivityFromSchedule(p_330034_.getDayTime(), p_330034_.getGameTime());
                                 mutableint.setValue(0);
                                 return true;
                             }
