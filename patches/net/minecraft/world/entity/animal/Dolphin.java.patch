--- a/net/minecraft/world/entity/animal/Dolphin.java
+++ b/net/minecraft/world/entity/animal/Dolphin.java
@@ -66,7 +_,7 @@
     static final TargetingConditions SWIM_WITH_PLAYER_TARGETING = TargetingConditions.forNonCombat().range(10.0).ignoreLineOfSight();
     public static final int TOTAL_AIR_SUPPLY = 4800;
     private static final int TOTAL_MOISTNESS_LEVEL = 2400;
-    public static final Predicate<ItemEntity> ALLOWED_ITEMS = p_340107_ -> !p_340107_.hasPickUpDelay() && p_340107_.isAlive() && p_340107_.isInWater();
+    public static final Predicate<ItemEntity> ALLOWED_ITEMS = p_337826_ -> !p_337826_.hasPickUpDelay() && p_337826_.isAlive() && p_337826_.isInWater();
 
     public Dolphin(EntityType<? extends Dolphin> p_28316_, Level p_28317_) {
         super(p_28316_, p_28317_);
@@ -205,11 +_,7 @@
     @Override
     public boolean canTakeItem(ItemStack p_28376_) {
         EquipmentSlot equipmentslot = Mob.getEquipmentSlotForItem(p_28376_);
-        if (!this.getItemBySlot(equipmentslot).isEmpty()) {
-            return false;
-        } else {
-            return equipmentslot == EquipmentSlot.MAINHAND && super.canTakeItem(p_28376_);
-        }
+        return !this.getItemBySlot(equipmentslot).isEmpty() ? false : equipmentslot == EquipmentSlot.MAINHAND && super.canTakeItem(p_28376_);
     }
 
     @Override
@@ -257,7 +_,7 @@
                 float f1 = Mth.sin(this.getYRot() * (float) (Math.PI / 180.0)) * 0.3F;
                 float f2 = 1.2F - this.random.nextFloat() * 0.7F;
 
-                for(int i = 0; i < 2; ++i) {
+                for (int i = 0; i < 2; i++) {
                     this.level()
                         .addParticle(
                             ParticleTypes.DOLPHIN,
@@ -293,7 +_,7 @@
     }
 
     private void addParticlesAroundSelf(ParticleOptions p_28338_) {
-        for(int i = 0; i < 7; ++i) {
+        for (int i = 0; i < 7; i++) {
             double d0 = this.random.nextGaussian() * 0.01;
             double d1 = this.random.nextGaussian() * 0.01;
             double d2 = this.random.nextGaussian() * 0.01;
@@ -469,11 +_,7 @@
         @Override
         public boolean canUse() {
             this.player = this.dolphin.level().getNearestPlayer(Dolphin.SWIM_WITH_PLAYER_TARGETING, this.dolphin);
-            if (this.player == null) {
-                return false;
-            } else {
-                return this.player.isSwimming() && this.dolphin.getTarget() != this.player;
-            }
+            return this.player == null ? false : this.player.isSwimming() && this.dolphin.getTarget() != this.player;
         }
 
         @Override
