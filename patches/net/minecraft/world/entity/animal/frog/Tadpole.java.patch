--- a/net/minecraft/world/entity/animal/frog/Tadpole.java
+++ b/net/minecraft/world/entity/animal/frog/Tadpole.java
@@ -227,8 +_,10 @@
     }
 
     private void ageUp() {
+        if (!net.neoforged.neoforge.event.EventHooks.canLivingConvert(this, EntityType.FROG, (timer) -> {})) return;
         if (this.level() instanceof ServerLevel serverlevel) {
             this.convertTo(EntityType.FROG, ConversionParams.single(this, false, false), p_419054_ -> {
+                net.neoforged.neoforge.event.EventHooks.onLivingConvert(this, p_419054_);
                 p_419054_.finalizeSpawn(serverlevel, this.level().getCurrentDifficultyAt(p_419054_.blockPosition()), EntitySpawnReason.CONVERSION, null);
                 p_419054_.setPersistenceRequired();
                 p_419054_.fudgePositionAfterSizeChange(this.getDimensions(this.getPose()));
