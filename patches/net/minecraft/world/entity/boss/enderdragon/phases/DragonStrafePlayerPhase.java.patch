--- a/net/minecraft/world/entity/boss/enderdragon/phases/DragonStrafePlayerPhase.java
+++ b/net/minecraft/world/entity/boss/enderdragon/phases/DragonStrafePlayerPhase.java
@@ -52,7 +_,7 @@
             double d13 = 64.0;
             if (this.attackTarget.distanceToSqr(this.dragon) < 4096.0) {
                 if (this.dragon.hasLineOfSight(this.attackTarget)) {
-                    ++this.fireballCharge;
+                    this.fireballCharge++;
                     Vec3 vec31 = new Vec3(this.attackTarget.getX() - this.dragon.getX(), 0.0, this.attackTarget.getZ() - this.dragon.getZ()).normalize();
                     Vec3 vec3 = new Vec3(
                             (double)Mth.sin(this.dragon.getYRot() * (float) (Math.PI / 180.0)),
@@ -81,7 +_,7 @@
                         this.dragon.level().addFreshEntity(dragonfireball);
                         this.fireballCharge = 0;
                         if (this.currentPath != null) {
-                            while(!this.currentPath.isDone()) {
+                            while (!this.currentPath.isDone()) {
                                 this.currentPath.advance();
                             }
                         }
@@ -89,10 +_,10 @@
                         this.dragon.getPhaseManager().setPhase(EnderDragonPhase.HOLDING_PATTERN);
                     }
                 } else if (this.fireballCharge > 0) {
-                    --this.fireballCharge;
+                    this.fireballCharge--;
                 }
             } else if (this.fireballCharge > 0) {
-                --this.fireballCharge;
+                this.fireballCharge--;
             }
         }
     }
@@ -107,9 +_,9 @@
             }
 
             if (this.holdingPatternClockwise) {
-                ++j;
+                j++;
             } else {
-                --j;
+                j--;
             }
 
             if (this.dragon.getDragonFight() != null && this.dragon.getDragonFight().getCrystalsAlive() > 0) {
@@ -142,7 +_,7 @@
             double d1;
             do {
                 d1 = (double)((float)vec3i.getY() + this.dragon.getRandom().nextFloat() * 20.0F);
-            } while(d1 < (double)vec3i.getY());
+            } while (d1 < (double)vec3i.getY());
 
             this.targetLocation = new Vec3(d0, d1, d2);
         }
