--- a/net/minecraft/world/grid/GridCarrier.java
+++ /dev/null
@@ -1,215 +_,0 @@
-package net.minecraft.world.grid;
-
-import javax.annotation.Nullable;
-import net.minecraft.core.BlockPos;
-import net.minecraft.core.Direction;
-import net.minecraft.core.registries.Registries;
-import net.minecraft.nbt.CompoundTag;
-import net.minecraft.network.protocol.Packet;
-import net.minecraft.network.protocol.game.ClientGamePacketListener;
-import net.minecraft.network.protocol.game.ClientboundAddSubGridPacket;
-import net.minecraft.network.syncher.EntityDataAccessor;
-import net.minecraft.network.syncher.EntityDataSerializers;
-import net.minecraft.network.syncher.SynchedEntityData;
-import net.minecraft.resources.ResourceKey;
-import net.minecraft.resources.ResourceLocation;
-import net.minecraft.world.entity.Entity;
-import net.minecraft.world.entity.EntityType;
-import net.minecraft.world.level.Level;
-import net.minecraft.world.phys.Vec3;
-
-public class GridCarrier extends Entity {
-    public static final int LERP_STEPS = 2;
-    private static final EntityDataAccessor<Direction> MOVEMENT_DIRECTION = SynchedEntityData.defineId(GridCarrier.class, EntityDataSerializers.DIRECTION);
-    private static final EntityDataAccessor<Float> MOVEMENT_SPEED = SynchedEntityData.defineId(GridCarrier.class, EntityDataSerializers.FLOAT);
-    private final SubGrid grid;
-    @Nullable
-    private SubGridMovementCollider movementCollider;
-    @Nullable
-    private GridCarrier.PosInterpolationTarget posInterpolationTarget;
-    private int placeInTicks;
-
-    public GridCarrier(EntityType<?> p_340631_, Level p_341187_) {
-        super(p_340631_, p_341187_);
-        this.grid = p_341187_.createSubGrid(this);
-        this.noCulling = true;
-    }
-
-    public void setMovement(Direction p_342285_, float p_341245_) {
-        this.getEntityData().set(MOVEMENT_DIRECTION, p_342285_);
-        this.getEntityData().set(MOVEMENT_SPEED, p_341245_);
-        this.movementCollider = SubGridMovementCollider.generate(this.grid.getBlocks(), p_342285_);
-    }
-
-    public void clearMovement() {
-        this.getEntityData().set(MOVEMENT_SPEED, 0.0F);
-        this.movementCollider = null;
-    }
-
-    public SubGrid grid() {
-        return this.grid;
-    }
-
-    @Override
-    public void setPos(double p_340776_, double p_342430_, double p_342380_) {
-        super.setPos(p_340776_, p_342430_, p_342380_);
-        if (this.grid != null) {
-            this.grid.updatePosition(p_340776_, p_342430_, p_342380_);
-        }
-    }
-
-    @Override
-    public void tick() {
-        super.tick();
-        Direction direction = this.getMovementDirection();
-        this.grid.getBlocks().tick(this.level(), this.position(), direction);
-        if (this.level().isClientSide()) {
-            this.tickClient();
-        } else {
-            this.tickServer();
-        }
-    }
-
-    private void tickClient() {
-        if (this.posInterpolationTarget != null) {
-            this.posInterpolationTarget.applyLerpStep(this);
-            if (--this.posInterpolationTarget.steps == 0) {
-                this.posInterpolationTarget = null;
-            }
-        }
-    }
-
-    private void tickServer() {
-        Direction direction = this.getMovementDirection();
-        float f = this.getMovementSpeed();
-        if (this.placeInTicks == 0 && f == 0.0F) {
-            this.placeInTicks = 2;
-        }
-
-        if (this.placeInTicks > 0) {
-            --this.placeInTicks;
-            if (this.placeInTicks == 1) {
-                this.grid.getBlocks().place(this.blockPosition(), this.level());
-            } else if (this.placeInTicks == 0) {
-                this.discard();
-            }
-        } else if (this.movementCollider != null) {
-            this.tickMovement(this.movementCollider, direction, f);
-        }
-    }
-
-    private void tickMovement(SubGridMovementCollider p_341312_, Direction p_342516_, float p_341367_) {
-        Vec3 vec3 = this.position();
-        Vec3 vec31 = vec3.add(
-            (double)((float)p_342516_.getStepX() * p_341367_),
-            (double)((float)p_342516_.getStepY() * p_341367_),
-            (double)((float)p_342516_.getStepZ() * p_341367_)
-        );
-        BlockPos blockpos = this.getCollidingPos(vec3, p_342516_);
-        BlockPos blockpos1 = this.getCollidingPos(vec31, p_342516_);
-        BlockPos.MutableBlockPos blockpos$mutableblockpos = blockpos.mutable();
-
-        while(!blockpos$mutableblockpos.equals(blockpos1)) {
-            blockpos$mutableblockpos.move(p_342516_);
-            if (p_341312_.checkCollision(this.level(), blockpos$mutableblockpos)) {
-                BlockPos blockpos2 = blockpos$mutableblockpos.relative(p_342516_, -1);
-                this.setPos(Vec3.atLowerCornerOf(blockpos2));
-                this.clearMovement();
-                this.placeInTicks = 5;
-                return;
-            }
-        }
-
-        this.setPos(vec31);
-    }
-
-    private BlockPos getCollidingPos(Vec3 p_341107_, Direction p_342029_) {
-        BlockPos blockpos = BlockPos.containing(p_341107_);
-        return p_342029_.getAxisDirection() == Direction.AxisDirection.POSITIVE ? blockpos.relative(p_342029_) : blockpos;
-    }
-
-    @Override
-    protected void defineSynchedData(SynchedEntityData.Builder p_342221_) {
-        p_342221_.define(MOVEMENT_DIRECTION, Direction.NORTH);
-        p_342221_.define(MOVEMENT_SPEED, 0.0F);
-    }
-
-    @Override
-    protected void readAdditionalSaveData(CompoundTag p_341558_) {
-        this.grid.setBlocks(SubGridBlocks.decode(this.registryAccess().lookupOrThrow(Registries.BLOCK), p_341558_.getCompound("blocks")));
-        if (p_341558_.contains("biome", 8)) {
-            this.registryAccess()
-                .registryOrThrow(Registries.BIOME)
-                .getHolder(new ResourceLocation(p_341558_.getString("biome")))
-                .ifPresent(this.grid::setBiome);
-        }
-
-        if (p_341558_.contains("movement_direction", 8)) {
-            Direction direction = Direction.byName(p_341558_.getString("movement_direction"));
-            if (direction != null) {
-                this.setMovement(direction, p_341558_.getFloat("movement_speed"));
-            }
-        } else {
-            this.clearMovement();
-        }
-    }
-
-    @Override
-    protected void addAdditionalSaveData(CompoundTag p_341343_) {
-        p_341343_.put("blocks", this.grid.getBlocks().encode());
-        this.grid.getBiome().unwrapKey().ifPresent(p_340387_ -> p_341343_.putString("biome", p_340387_.location().toString()));
-        p_341343_.putString("movement_direction", this.getMovementDirection().getSerializedName());
-        p_341343_.putFloat("movement_speed", this.getMovementSpeed());
-    }
-
-    private float getMovementSpeed() {
-        return this.getEntityData().get(MOVEMENT_SPEED);
-    }
-
-    private Direction getMovementDirection() {
-        return this.getEntityData().get(MOVEMENT_DIRECTION);
-    }
-
-    @Override
-    public Packet<ClientGamePacketListener> getAddEntityPacket() {
-        return new ClientboundAddSubGridPacket(this);
-    }
-
-    @Override
-    public void lerpTo(double p_341499_, double p_341679_, double p_342405_, float p_340372_, float p_340575_, int p_342491_) {
-        this.posInterpolationTarget = new GridCarrier.PosInterpolationTarget(2, p_341499_, p_341679_, p_342405_);
-    }
-
-    @Override
-    public double lerpTargetX() {
-        return this.posInterpolationTarget != null ? this.posInterpolationTarget.targetX : this.getX();
-    }
-
-    @Override
-    public double lerpTargetY() {
-        return this.posInterpolationTarget != null ? this.posInterpolationTarget.targetY : this.getY();
-    }
-
-    @Override
-    public double lerpTargetZ() {
-        return this.posInterpolationTarget != null ? this.posInterpolationTarget.targetZ : this.getZ();
-    }
-
-    static class PosInterpolationTarget {
-        int steps;
-        final double targetX;
-        final double targetY;
-        final double targetZ;
-
-        PosInterpolationTarget(int p_341794_, double p_342470_, double p_340407_, double p_342096_) {
-            this.steps = p_341794_;
-            this.targetX = p_342470_;
-            this.targetY = p_340407_;
-            this.targetZ = p_342096_;
-        }
-
-        void applyLerpStep(Entity p_341880_) {
-            p_341880_.lerpPositionAndRotationStep(this.steps, this.targetX, this.targetY, this.targetZ, 0.0, 0.0);
-        }
-    }
-}
