--- a/net/minecraft/world/grid/SubGridMovementCollider.java
+++ /dev/null
@@ -1,87 +_,0 @@
-package net.minecraft.world.grid;
-
-import it.unimi.dsi.fastutil.longs.LongArrayList;
-import it.unimi.dsi.fastutil.longs.LongIterator;
-import it.unimi.dsi.fastutil.longs.LongList;
-import net.minecraft.core.BlockPos;
-import net.minecraft.core.Direction;
-import net.minecraft.world.level.Level;
-import net.minecraft.world.level.block.state.BlockState;
-
-public class SubGridMovementCollider {
-    private final LongList edgeBlocks;
-    private final BlockPos size;
-
-    private SubGridMovementCollider(LongList p_342078_, BlockPos p_342335_) {
-        this.edgeBlocks = p_342078_;
-        this.size = p_342335_;
-    }
-
-    public static SubGridMovementCollider generate(SubGridBlocks p_340588_, Direction p_342560_) {
-        LongList longlist = new LongArrayList();
-        BlockPos.MutableBlockPos blockpos$mutableblockpos = new BlockPos.MutableBlockPos();
-        Direction direction = p_342560_.getOpposite();
-
-        for(BlockPos blockpos : getFrontSide(p_340588_, p_342560_)) {
-            blockpos$mutableblockpos.set(blockpos);
-            boolean flag = false;
-            int i = p_342560_.getAxis().choose(p_340588_.sizeX(), p_340588_.sizeY(), p_340588_.sizeZ());
-
-            for(int j = 0; j < i; ++j) {
-                BlockState blockstate = p_340588_.getBlockState(blockpos$mutableblockpos);
-                if (isCollidable(blockstate)) {
-                    if (!flag) {
-                        longlist.add(blockpos$mutableblockpos.asLong());
-                    }
-
-                    flag = true;
-                } else {
-                    flag = false;
-                }
-
-                blockpos$mutableblockpos.move(direction);
-            }
-        }
-
-        return new SubGridMovementCollider(longlist, new BlockPos(p_340588_.sizeX(), p_340588_.sizeY(), p_340588_.sizeZ()));
-    }
-
-    private static Iterable<BlockPos> getFrontSide(SubGridBlocks p_340623_, Direction p_341503_) {
-        BlockPos blockpos = new BlockPos(
-            Math.max(p_341503_.getStepX(), 0) * (p_340623_.sizeX() - 1),
-            Math.max(p_341503_.getStepY(), 0) * (p_340623_.sizeY() - 1),
-            Math.max(p_341503_.getStepZ(), 0) * (p_340623_.sizeZ() - 1)
-        );
-        BlockPos blockpos1 = blockpos.offset(
-            p_341503_.getAxis() == Direction.Axis.X ? 0 : p_340623_.sizeX() - 1,
-            p_341503_.getAxis() == Direction.Axis.Y ? 0 : p_340623_.sizeY() - 1,
-            p_341503_.getAxis() == Direction.Axis.Z ? 0 : p_340623_.sizeZ() - 1
-        );
-        return BlockPos.betweenClosed(blockpos, blockpos1);
-    }
-
-    public boolean checkCollision(Level p_340469_, BlockPos p_340571_) {
-        int i = p_340571_.getY();
-        int j = i + this.size.getY() - 1;
-        if (i >= p_340469_.getMinBuildHeight() && j < p_340469_.getMaxBuildHeight()) {
-            BlockPos.MutableBlockPos blockpos$mutableblockpos = new BlockPos.MutableBlockPos();
-            LongIterator longiterator = this.edgeBlocks.longIterator();
-
-            while(longiterator.hasNext()) {
-                blockpos$mutableblockpos.set(longiterator.nextLong());
-                blockpos$mutableblockpos.move(p_340571_);
-                if (isCollidable(p_340469_.getBlockState(blockpos$mutableblockpos))) {
-                    return true;
-                }
-            }
-
-            return false;
-        } else {
-            return true;
-        }
-    }
-
-    private static boolean isCollidable(BlockState p_340769_) {
-        return !p_340769_.canBeReplaced();
-    }
-}
