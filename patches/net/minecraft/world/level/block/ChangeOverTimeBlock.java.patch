--- a/net/minecraft/world/level/block/ChangeOverTimeBlock.java
+++ b/net/minecraft/world/level/block/ChangeOverTimeBlock.java
@@ -27,27 +_,24 @@
         int j = 0;
         int k = 0;
 
-        for(BlockPos blockpos : BlockPos.withinManhattan(p_309042_, 4, 4, 4)) {
+        for (BlockPos blockpos : BlockPos.withinManhattan(p_309042_, 4, 4, 4)) {
             int l = blockpos.distManhattan(p_309042_);
             if (l > 4) {
                 break;
             }
 
-            if (!blockpos.equals(p_309042_)) {
-                Block $$10 = p_309039_.getBlockState(blockpos).getBlock();
-                if ($$10 instanceof ChangeOverTimeBlock changeovertimeblock) {
-                    Enum<?> oenum = changeovertimeblock.getAge();
-                    if (this.getAge().getClass() == oenum.getClass()) {
-                        int i1 = oenum.ordinal();
-                        if (i1 < i) {
-                            return Optional.empty();
-                        }
+            if (!blockpos.equals(p_309042_) && p_309039_.getBlockState(blockpos).getBlock() instanceof ChangeOverTimeBlock<?> changeovertimeblock) {
+                Enum<?> oenum = changeovertimeblock.getAge();
+                if (this.getAge().getClass() == oenum.getClass()) {
+                    int i1 = oenum.ordinal();
+                    if (i1 < i) {
+                        return Optional.empty();
+                    }
 
-                        if (i1 > i) {
-                            ++k;
-                        } else {
-                            ++j;
-                        }
+                    if (i1 > i) {
+                        k++;
+                    } else {
+                        j++;
                     }
                 }
             }
