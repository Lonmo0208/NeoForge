--- a/net/minecraft/world/level/levelgen/feature/treedecorators/BeehiveDecorator.java
+++ b/net/minecraft/world/level/levelgen/feature/treedecorators/BeehiveDecorator.java
@@ -1,8 +_,7 @@
 package net.minecraft.world.level.levelgen.feature.treedecorators;
 
 import com.mojang.serialization.Codec;
-import com.mojang.serialization.codecs.RecordCodecBuilder;
-import com.mojang.serialization.codecs.RecordCodecBuilder.Instance;
+import com.mojang.serialization.MapCodec;
 import java.util.Collections;
 import java.util.List;
 import java.util.Optional;
@@ -17,24 +_,18 @@
 import net.minecraft.world.level.block.entity.BlockEntityType;
 
 public class BeehiveDecorator extends TreeDecorator {
-    public static final Codec<BeehiveDecorator> CODEC = RecordCodecBuilder.create(
-        p_340292_ -> p_340292_.group(
-                    Codec.floatRange(0.0F, 1.0F).fieldOf("probability").forGetter(p_69971_ -> p_69971_.probability),
-                    Codec.BOOL.fieldOf("fixed_height").orElse(true).forGetter(p_340293_ -> p_340293_.fixedHeight)
-                )
-                .apply(p_340292_, BeehiveDecorator::new)
-    );
+    public static final MapCodec<BeehiveDecorator> CODEC = Codec.floatRange(0.0F, 1.0F)
+        .fieldOf("probability")
+        .xmap(BeehiveDecorator::new, p_69971_ -> p_69971_.probability);
     private static final Direction WORLDGEN_FACING = Direction.SOUTH;
     private static final Direction[] SPAWN_DIRECTIONS = Direction.Plane.HORIZONTAL
         .stream()
         .filter(p_202307_ -> p_202307_ != WORLDGEN_FACING.getOpposite())
-        .toArray(p_202297_ -> new Direction[p_202297_]);
+        .toArray(Direction[]::new);
     private final float probability;
-    private final boolean fixedHeight;
 
-    public BeehiveDecorator(float p_69958_, boolean p_342593_) {
+    public BeehiveDecorator(float p_69958_) {
         this.probability = p_69958_;
-        this.fixedHeight = p_342593_;
     }
 
     @Override
@@ -48,35 +_,27 @@
         if (!(randomsource.nextFloat() >= this.probability)) {
             List<BlockPos> list = p_226019_.leaves();
             List<BlockPos> list1 = p_226019_.logs();
-            if (!list1.isEmpty()) {
-                int i;
-                if (this.fixedHeight) {
-                    i = !list.isEmpty()
-                        ? Math.max(list.get(0).getY() - 1, list1.get(0).getY() + 1)
-                        : Math.min(list1.get(0).getY() + 1 + randomsource.nextInt(3), list1.get(list1.size() - 1).getY());
-                } else {
-                    i = randomsource.nextIntBetweenInclusive(list1.get(0).getY() + 1, list1.get(list1.size() - 1).getY() - 1);
-                }
-
-                List<BlockPos> list2 = list1.stream()
-                    .filter(p_202300_ -> p_202300_.getY() == i)
-                    .flatMap(p_202305_ -> Stream.of(SPAWN_DIRECTIONS).map(p_202305_::relative))
-                    .collect(Collectors.toList());
-                if (!list2.isEmpty()) {
-                    Collections.shuffle(list2);
-                    Optional<BlockPos> optional = list2.stream()
-                        .filter(p_226022_ -> p_226019_.isAir(p_226022_) && p_226019_.isAir(p_226022_.relative(WORLDGEN_FACING)))
-                        .findFirst();
-                    if (!optional.isEmpty()) {
-                        p_226019_.setBlock(optional.get(), Blocks.BEE_NEST.defaultBlockState().setValue(BeehiveBlock.FACING, WORLDGEN_FACING));
-                        p_226019_.level().getBlockEntity(optional.get(), BlockEntityType.BEEHIVE).ifPresent(p_330149_ -> {
-                            int j = 2 + randomsource.nextInt(2);
-
-                            for(int k = 0; k < j; ++k) {
-                                p_330149_.storeBee(BeehiveBlockEntity.Occupant.create(randomsource.nextInt(599)));
-                            }
-                        });
-                    }
+            int i = !list.isEmpty()
+                ? Math.max(list.get(0).getY() - 1, list1.get(0).getY() + 1)
+                : Math.min(list1.get(0).getY() + 1 + randomsource.nextInt(3), list1.get(list1.size() - 1).getY());
+            List<BlockPos> list2 = list1.stream()
+                .filter(p_202300_ -> p_202300_.getY() == i)
+                .flatMap(p_202305_ -> Stream.of(SPAWN_DIRECTIONS).map(p_202305_::relative))
+                .collect(Collectors.toList());
+            if (!list2.isEmpty()) {
+                Collections.shuffle(list2);
+                Optional<BlockPos> optional = list2.stream()
+                    .filter(p_226022_ -> p_226019_.isAir(p_226022_) && p_226019_.isAir(p_226022_.relative(WORLDGEN_FACING)))
+                    .findFirst();
+                if (!optional.isEmpty()) {
+                    p_226019_.setBlock(optional.get(), Blocks.BEE_NEST.defaultBlockState().setValue(BeehiveBlock.FACING, WORLDGEN_FACING));
+                    p_226019_.level().getBlockEntity(optional.get(), BlockEntityType.BEEHIVE).ifPresent(p_330149_ -> {
+                        int j = 2 + randomsource.nextInt(2);
+
+                        for (int k = 0; k < j; k++) {
+                            p_330149_.storeBee(BeehiveBlockEntity.Occupant.create(randomsource.nextInt(599)));
+                        }
+                    });
                 }
             }
         }
