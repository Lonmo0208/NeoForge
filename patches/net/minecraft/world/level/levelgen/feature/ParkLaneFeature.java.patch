--- a/net/minecraft/world/level/levelgen/feature/ParkLaneFeature.java
+++ /dev/null
@@ -1,159 +_,0 @@
-package net.minecraft.world.level.levelgen.feature;
-
-import com.mojang.serialization.Codec;
-import java.util.ArrayList;
-import java.util.List;
-import java.util.function.Function;
-import net.minecraft.core.BlockPos;
-import net.minecraft.core.Direction;
-import net.minecraft.util.RandomSource;
-import net.minecraft.world.level.WorldGenLevel;
-import net.minecraft.world.level.block.Blocks;
-import net.minecraft.world.level.block.state.BlockState;
-import net.minecraft.world.level.levelgen.Heightmap;
-import net.minecraft.world.level.levelgen.feature.configurations.NoneFeatureConfiguration;
-
-public class ParkLaneFeature extends Feature<NoneFeatureConfiguration> {
-    public ParkLaneFeature(Codec<NoneFeatureConfiguration> p_340470_) {
-        super(p_340470_);
-    }
-
-    @Override
-    public boolean place(FeaturePlaceContext<NoneFeatureConfiguration> p_340551_) {
-        BlockPos blockpos = p_340551_.origin();
-        WorldGenLevel worldgenlevel = p_340551_.level();
-        RandomSource randomsource = p_340551_.random();
-        if (worldgenlevel.getHeightmapPos(Heightmap.Types.WORLD_SURFACE_WG, blockpos).getY() <= blockpos.getY() + 2) {
-            return false;
-        } else if (!this.isValidPlacementLocation(worldgenlevel, blockpos)) {
-            return false;
-        } else {
-            Direction direction = Direction.from2DDataValue(randomsource.nextInt(4));
-            BlockPos.MutableBlockPos blockpos$mutableblockpos = new BlockPos.MutableBlockPos();
-            worldgenlevel.setBlock(blockpos.below(), Blocks.POISON_PATH.defaultBlockState(), 2);
-            int i = randomsource.nextInt(6, 12);
-            List<BlockPos> list = new ArrayList<>();
-
-            for(int j = 0; j < 2; ++j) {
-                blockpos$mutableblockpos.set(blockpos);
-                float f = 1.0F;
-                int k = 0;
-                direction = direction.getOpposite();
-
-                while(randomsource.nextFloat() < f) {
-                    if (++k > 2) {
-                        break;
-                    }
-
-                    blockpos$mutableblockpos.move(direction);
-                    if (!this.isValidPlacementLocation(worldgenlevel, blockpos$mutableblockpos)) {
-                        break;
-                    }
-
-                    f *= 0.8F;
-                    Function<BlockPos, BlockState> function = p_341849_ -> {
-                        list.add(p_341849_.immutable());
-                        return Blocks.AIR.defaultBlockState();
-                    };
-                    Function<BlockPos, BlockState> function1 = p_341068_ -> Blocks.POISON_PATH.defaultBlockState();
-                    this.runRowOfPotatoes(worldgenlevel, randomsource, blockpos$mutableblockpos, direction.getClockWise(), function1, function, i);
-                    Direction direction1 = direction.getCounterClockWise();
-                    this.runRowOfPotatoes(worldgenlevel, randomsource, blockpos$mutableblockpos.relative(direction1), direction1, function1, function, i);
-                }
-
-                if (randomsource.nextInt(2) == 0) {
-                    blockpos$mutableblockpos.move(direction);
-                    if (this.isValidPlacementLocation(worldgenlevel, blockpos$mutableblockpos)) {
-                        Function<BlockPos, BlockState> function2 = p_341284_ -> {
-                            list.add(p_341284_.immutable());
-                            if (randomsource.nextInt(10) == 0) {
-                                worldgenlevel.setBlock(p_341284_.above(), Blocks.LANTERN.defaultBlockState(), 3);
-                            }
-
-                            return Blocks.POTATO_FENCE.defaultBlockState();
-                        };
-                        this.runRowOfPotatoes(
-                            worldgenlevel,
-                            randomsource,
-                            blockpos$mutableblockpos,
-                            direction.getClockWise(),
-                            p_340358_ -> Blocks.PEELGRASS_BLOCK.defaultBlockState(),
-                            function2,
-                            i
-                        );
-                        Direction direction3 = direction.getCounterClockWise();
-                        this.runRowOfPotatoes(
-                            worldgenlevel,
-                            randomsource,
-                            blockpos$mutableblockpos.relative(direction3),
-                            direction3,
-                            p_340872_ -> Blocks.PEELGRASS_BLOCK.defaultBlockState(),
-                            function2,
-                            i
-                        );
-                    }
-                }
-            }
-
-            for(BlockPos blockpos1 : list) {
-                worldgenlevel.getChunk(blockpos1).markPosForPostprocessing(blockpos1);
-            }
-
-            Direction direction2 = direction.getCounterClockWise();
-            this.runRowOfPotatoes(
-                worldgenlevel,
-                randomsource,
-                blockpos.relative(direction2),
-                direction2,
-                p_341255_ -> Blocks.POISON_PATH.defaultBlockState(),
-                p_340483_ -> Blocks.AIR.defaultBlockState(),
-                i
-            );
-            direction2 = direction2.getOpposite();
-            this.runRowOfPotatoes(
-                worldgenlevel,
-                randomsource,
-                blockpos.relative(direction2),
-                direction2,
-                p_341657_ -> Blocks.POISON_PATH.defaultBlockState(),
-                p_341476_ -> Blocks.AIR.defaultBlockState(),
-                i
-            );
-            return false;
-        }
-    }
-
-    private void runRowOfPotatoes(
-        WorldGenLevel p_340783_,
-        RandomSource p_340902_,
-        BlockPos p_341315_,
-        Direction p_341646_,
-        Function<BlockPos, BlockState> p_340675_,
-        Function<BlockPos, BlockState> p_341615_,
-        int p_341261_
-    ) {
-        BlockPos.MutableBlockPos blockpos$mutableblockpos = new BlockPos.MutableBlockPos(p_341315_);
-        int i = p_341261_ + p_340902_.nextInt(3);
-
-        for(int j = 0; j < i; ++j) {
-            if (!this.isValidPlacementLocation(p_340783_, blockpos$mutableblockpos)) {
-                blockpos$mutableblockpos.move(Direction.UP);
-                if (!this.isValidPlacementLocation(p_340783_, blockpos$mutableblockpos)) {
-                    blockpos$mutableblockpos.move(Direction.DOWN, 2);
-                    if (!this.isValidPlacementLocation(p_340783_, blockpos$mutableblockpos)) {
-                        break;
-                    }
-                }
-            }
-
-            p_340783_.setBlock(blockpos$mutableblockpos.below(), p_340675_.apply(blockpos$mutableblockpos.below()), 3);
-            p_340783_.setBlock(blockpos$mutableblockpos, p_341615_.apply(blockpos$mutableblockpos), 3);
-            blockpos$mutableblockpos.move(p_341646_);
-        }
-    }
-
-    private boolean isValidPlacementLocation(WorldGenLevel p_340977_, BlockPos p_341943_) {
-        return (p_340977_.isEmptyBlock(p_341943_) || p_340977_.getBlockState(p_341943_).is(Blocks.POTATO_FENCE))
-            && p_340977_.getBlockState(p_341943_.below()).is(Blocks.PEELGRASS_BLOCK);
-    }
-}
