--- a/net/minecraft/world/level/levelgen/heightproviders/HeightProviderType.java
+++ b/net/minecraft/world/level/levelgen/heightproviders/HeightProviderType.java
@@ -1,6 +_,6 @@
 package net.minecraft.world.level.levelgen.heightproviders;
 
-import com.mojang.serialization.Codec;
+import com.mojang.serialization.MapCodec;
 import net.minecraft.core.Registry;
 import net.minecraft.core.registries.BuiltInRegistries;
 
@@ -12,9 +_,9 @@
     HeightProviderType<TrapezoidHeight> TRAPEZOID = register("trapezoid", TrapezoidHeight.CODEC);
     HeightProviderType<WeightedListHeight> WEIGHTED_LIST = register("weighted_list", WeightedListHeight.CODEC);
 
-    Codec<P> codec();
+    MapCodec<P> codec();
 
-    private static <P extends HeightProvider> HeightProviderType<P> register(String p_161990_, Codec<P> p_161991_) {
-        return Registry.register(BuiltInRegistries.HEIGHT_PROVIDER_TYPE, p_161990_, () -> p_161991_);
+    private static <P extends HeightProvider> HeightProviderType<P> register(String p_161990_, MapCodec<P> p_338570_) {
+        return Registry.register(BuiltInRegistries.HEIGHT_PROVIDER_TYPE, p_161990_, () -> p_338570_);
     }
 }
