--- a/net/minecraft/world/level/levelgen/presets/WorldPreset.java
+++ b/net/minecraft/world/level/levelgen/presets/WorldPreset.java
@@ -13,22 +_,19 @@
 import net.minecraft.core.registries.Registries;
 import net.minecraft.resources.RegistryFileCodec;
 import net.minecraft.resources.ResourceKey;
-import net.minecraft.util.ExtraCodecs;
 import net.minecraft.world.level.dimension.LevelStem;
 import net.minecraft.world.level.levelgen.WorldDimensions;
 
 public class WorldPreset {
-    public static final Codec<WorldPreset> DIRECT_CODEC = ExtraCodecs.validate(
-        RecordCodecBuilder.create(
+    public static final Codec<WorldPreset> DIRECT_CODEC = RecordCodecBuilder.<WorldPreset>create(
             p_259011_ -> p_259011_.group(
                         Codec.unboundedMap(ResourceKey.codec(Registries.LEVEL_STEM), LevelStem.CODEC)
                             .fieldOf("dimensions")
                             .forGetter(p_226430_ -> p_226430_.dimensions)
                     )
                     .apply(p_259011_, WorldPreset::new)
-        ),
-        WorldPreset::requireOverworld
-    );
+        )
+        .validate(WorldPreset::requireOverworld);
     public static final Codec<Holder<WorldPreset>> CODEC = RegistryFileCodec.create(Registries.WORLD_PRESET, DIRECT_CODEC);
     private final Map<ResourceKey<LevelStem>, LevelStem> dimensions;
 
@@ -41,7 +_,7 @@
         WorldDimensions.keysInOrder(this.dimensions.keySet().stream()).forEach(p_325910_ -> {
             LevelStem levelstem = this.dimensions.get(p_325910_);
             if (levelstem != null) {
-                builder.put(p_325910_, levelstem);
+                builder.put((ResourceKey<LevelStem>)p_325910_, levelstem);
             }
         });
         return builder.build();
