--- a/net/minecraft/world/level/storage/loot/entries/LootPoolEntryContainer.java
+++ b/net/minecraft/world/level/storage/loot/entries/LootPoolEntryContainer.java
@@ -7,7 +_,6 @@
 import java.util.List;
 import java.util.function.Predicate;
 import net.minecraft.Util;
-import net.minecraft.util.ExtraCodecs;
 import net.minecraft.world.level.storage.loot.LootContext;
 import net.minecraft.world.level.storage.loot.ValidationContext;
 import net.minecraft.world.level.storage.loot.predicates.ConditionUserBuilder;
@@ -24,13 +_,11 @@
     }
 
     protected static <T extends LootPoolEntryContainer> P1<Mu<T>, List<LootItemCondition>> commonFields(Instance<T> p_299092_) {
-        return p_299092_.group(
-            ExtraCodecs.strictOptionalField(LootItemConditions.DIRECT_CODEC.listOf(), "conditions", List.of()).forGetter(p_298548_ -> p_298548_.conditions)
-        );
+        return p_299092_.group(LootItemConditions.DIRECT_CODEC.listOf().optionalFieldOf("conditions", List.of()).forGetter(p_298548_ -> p_298548_.conditions));
     }
 
     public void validate(ValidationContext p_79641_) {
-        for(int i = 0; i < this.conditions.size(); ++i) {
+        for (int i = 0; i < this.conditions.size(); i++) {
             this.conditions.get(i).validate(p_79641_.forChild(".condition[" + i + "]"));
         }
     }
