--- a/net/minecraft/world/inventory/PoisonousPotatoCutterMenu.java
+++ /dev/null
@@ -1,238 +_,0 @@
-package net.minecraft.world.inventory;
-
-import com.google.common.collect.Lists;
-import java.util.List;
-import net.minecraft.core.BlockPos;
-import net.minecraft.sounds.SoundEvents;
-import net.minecraft.sounds.SoundSource;
-import net.minecraft.world.Container;
-import net.minecraft.world.SimpleContainer;
-import net.minecraft.world.entity.player.Inventory;
-import net.minecraft.world.entity.player.Player;
-import net.minecraft.world.item.Item;
-import net.minecraft.world.item.ItemStack;
-import net.minecraft.world.item.crafting.PoisonousPotatoCutterRecipe;
-import net.minecraft.world.item.crafting.RecipeHolder;
-import net.minecraft.world.item.crafting.RecipeType;
-import net.minecraft.world.level.Level;
-import net.minecraft.world.level.block.Blocks;
-
-public class PoisonousPotatoCutterMenu extends AbstractContainerMenu {
-    public static final int INPUT_SLOT = 0;
-    public static final int RESULT_SLOT = 1;
-    private static final int INV_SLOT_START = 2;
-    private static final int INV_SLOT_END = 29;
-    private static final int USE_ROW_SLOT_START = 29;
-    private static final int USE_ROW_SLOT_END = 38;
-    private final ContainerLevelAccess access;
-    private final DataSlot selectedRecipeIndex = DataSlot.standalone();
-    private final Level level;
-    private List<RecipeHolder<PoisonousPotatoCutterRecipe>> recipes = Lists.newArrayList();
-    private ItemStack input = ItemStack.EMPTY;
-    long lastSoundTime;
-    final Slot inputSlot;
-    final Slot resultSlot;
-    Runnable slotUpdateListener = () -> {
-    };
-    public final Container container = new SimpleContainer(1) {
-        @Override
-        public void setChanged() {
-            super.setChanged();
-            PoisonousPotatoCutterMenu.this.slotsChanged(this);
-            PoisonousPotatoCutterMenu.this.slotUpdateListener.run();
-        }
-    };
-    final ResultContainer resultContainer = new ResultContainer();
-
-    public PoisonousPotatoCutterMenu(int p_340662_, Inventory p_342280_) {
-        this(p_340662_, p_342280_, ContainerLevelAccess.NULL);
-    }
-
-    public PoisonousPotatoCutterMenu(int p_340465_, Inventory p_341111_, final ContainerLevelAccess p_341250_) {
-        super(MenuType.STONECUTTER, p_340465_);
-        this.access = p_341250_;
-        this.level = p_341111_.player.level();
-        this.inputSlot = this.addSlot(new Slot(this.container, 0, 20, 33));
-        this.resultSlot = this.addSlot(new Slot(this.resultContainer, 1, 143, 33) {
-            @Override
-            public boolean mayPlace(ItemStack p_342569_) {
-                return false;
-            }
-
-            @Override
-            public void onTake(Player p_342492_, ItemStack p_341098_) {
-                p_341098_.onCraftedBy(p_342492_.level(), p_342492_, p_341098_.getCount());
-                PoisonousPotatoCutterMenu.this.resultContainer.awardUsedRecipes(p_342492_, this.getRelevantItems());
-                ItemStack itemstack = PoisonousPotatoCutterMenu.this.inputSlot.remove(1);
-                if (!itemstack.isEmpty()) {
-                    PoisonousPotatoCutterMenu.this.setupResultSlot();
-                }
-
-                p_341250_.execute((p_341876_, p_342089_) -> {
-                    long l = p_341876_.getGameTime();
-                    if (PoisonousPotatoCutterMenu.this.lastSoundTime != l) {
-                        p_341876_.playSound(null, p_342089_, SoundEvents.UI_STONECUTTER_TAKE_RESULT, SoundSource.BLOCKS, 1.0F, 1.0F);
-                        PoisonousPotatoCutterMenu.this.lastSoundTime = l;
-                    }
-                });
-                super.onTake(p_342492_, p_341098_);
-            }
-
-            private List<ItemStack> getRelevantItems() {
-                return List.of(PoisonousPotatoCutterMenu.this.inputSlot.getItem());
-            }
-        });
-
-        for(int i = 0; i < 3; ++i) {
-            for(int j = 0; j < 9; ++j) {
-                this.addSlot(new Slot(p_341111_, j + i * 9 + 9, 8 + j * 18, 84 + i * 18));
-            }
-        }
-
-        for(int k = 0; k < 9; ++k) {
-            this.addSlot(new Slot(p_341111_, k, 8 + k * 18, 142));
-        }
-
-        this.addDataSlot(this.selectedRecipeIndex);
-    }
-
-    public int getSelectedRecipeIndex() {
-        return this.selectedRecipeIndex.get();
-    }
-
-    public List<RecipeHolder<PoisonousPotatoCutterRecipe>> getRecipes() {
-        return this.recipes;
-    }
-
-    public int getNumRecipes() {
-        return this.recipes.size();
-    }
-
-    public boolean hasInputItem() {
-        return this.inputSlot.hasItem() && !this.recipes.isEmpty();
-    }
-
-    @Override
-    public boolean stillValid(Player p_341921_) {
-        return stillValid(this.access, p_341921_, Blocks.POISONOUS_POTATO_CUTTER);
-    }
-
-    @Override
-    public boolean clickMenuButton(Player p_340917_, int p_342131_) {
-        if (this.isValidRecipeIndex(p_342131_)) {
-            this.selectedRecipeIndex.set(p_342131_);
-            this.setupResultSlot();
-        }
-
-        return true;
-    }
-
-    private boolean isValidRecipeIndex(int p_341428_) {
-        return p_341428_ >= 0 && p_341428_ < this.recipes.size();
-    }
-
-    @Override
-    public void slotsChanged(Container p_342533_) {
-        ItemStack itemstack = this.inputSlot.getItem();
-        if (!itemstack.is(this.input.getItem())) {
-            this.input = itemstack.copy();
-            this.setupRecipeList(p_342533_, itemstack);
-        }
-    }
-
-    private void setupRecipeList(Container p_341521_, ItemStack p_340638_) {
-        this.recipes.clear();
-        this.selectedRecipeIndex.set(-1);
-        this.resultSlot.set(ItemStack.EMPTY);
-        if (!p_340638_.isEmpty()) {
-            this.recipes = this.level.getRecipeManager().getRecipesFor(RecipeType.POISONOUS_POTATO_CUTTING, p_341521_, this.level);
-        }
-    }
-
-    void setupResultSlot() {
-        if (!this.recipes.isEmpty() && this.isValidRecipeIndex(this.selectedRecipeIndex.get())) {
-            RecipeHolder<PoisonousPotatoCutterRecipe> recipeholder = this.recipes.get(this.selectedRecipeIndex.get());
-            ItemStack itemstack = recipeholder.value().assemble(this.container, this.level.registryAccess());
-            if (itemstack.isItemEnabled(this.level.enabledFeatures())) {
-                this.resultContainer.setRecipeUsed(recipeholder);
-                this.resultSlot.set(itemstack);
-            } else {
-                this.resultSlot.set(ItemStack.EMPTY);
-            }
-        } else {
-            this.resultSlot.set(ItemStack.EMPTY);
-        }
-
-        this.broadcastChanges();
-    }
-
-    @Override
-    public MenuType<?> getType() {
-        return MenuType.POISONOUS_POTATO_CUTTER;
-    }
-
-    public void registerUpdateListener(Runnable p_342030_) {
-        this.slotUpdateListener = p_342030_;
-    }
-
-    @Override
-    public boolean canTakeItemForPickAll(ItemStack p_341810_, Slot p_341151_) {
-        return p_341151_.container != this.resultContainer && super.canTakeItemForPickAll(p_341810_, p_341151_);
-    }
-
-    @Override
-    public ItemStack quickMoveStack(Player p_341462_, int p_341748_) {
-        ItemStack itemstack = ItemStack.EMPTY;
-        Slot slot = this.slots.get(p_341748_);
-        if (slot != null && slot.hasItem()) {
-            ItemStack itemstack1 = slot.getItem();
-            Item item = itemstack1.getItem();
-            itemstack = itemstack1.copy();
-            if (p_341748_ == 1) {
-                item.onCraftedBy(itemstack1, p_341462_.level(), p_341462_);
-                if (!this.moveItemStackTo(itemstack1, 2, 38, true)) {
-                    return ItemStack.EMPTY;
-                }
-
-                slot.onQuickCraft(itemstack1, itemstack);
-            } else if (p_341748_ == 0) {
-                if (!this.moveItemStackTo(itemstack1, 2, 38, false)) {
-                    return ItemStack.EMPTY;
-                }
-            } else if (this.level.getRecipeManager().getRecipeFor(RecipeType.POISONOUS_POTATO_CUTTING, new SimpleContainer(itemstack1), this.level).isPresent()
-                )
-             {
-                if (!this.moveItemStackTo(itemstack1, 0, 1, false)) {
-                    return ItemStack.EMPTY;
-                }
-            } else if (p_341748_ >= 2 && p_341748_ < 29) {
-                if (!this.moveItemStackTo(itemstack1, 29, 38, false)) {
-                    return ItemStack.EMPTY;
-                }
-            } else if (p_341748_ >= 29 && p_341748_ < 38 && !this.moveItemStackTo(itemstack1, 2, 29, false)) {
-                return ItemStack.EMPTY;
-            }
-
-            if (itemstack1.isEmpty()) {
-                slot.setByPlayer(ItemStack.EMPTY);
-            }
-
-            slot.setChanged();
-            if (itemstack1.getCount() == itemstack.getCount()) {
-                return ItemStack.EMPTY;
-            }
-
-            slot.onTake(p_341462_, itemstack1);
-            this.broadcastChanges();
-        }
-
-        return itemstack;
-    }
-
-    @Override
-    public void removed(Player p_340401_) {
-        super.removed(p_340401_);
-        this.resultContainer.removeItemNoUpdate(1);
-        this.access.execute((p_342232_, p_341790_) -> this.clearContainer(p_340401_, this.container));
-    }
-}
