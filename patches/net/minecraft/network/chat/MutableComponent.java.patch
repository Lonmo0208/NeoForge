--- a/net/minecraft/network/chat/MutableComponent.java
+++ b/net/minecraft/network/chat/MutableComponent.java
@@ -1,7 +_,6 @@
 package net.minecraft.network.chat;
 
 import com.google.common.collect.Lists;
-import java.util.Arrays;
 import java.util.List;
 import java.util.Objects;
 import java.util.function.UnaryOperator;
@@ -52,25 +_,8 @@
         return this.append(Component.literal(p_130947_));
     }
 
-    public MutableComponent append(Component... p_342598_) {
-        this.siblings.addAll(Arrays.asList(p_342598_));
-        return this;
-    }
-
-    public MutableComponent append(Object... p_342091_) {
-        for(Object object : p_342091_) {
-            if (object instanceof Component component) {
-                this.append(component);
-            } else {
-                if (!(object instanceof String)) {
-                    throw new IllegalArgumentException("Don't know how to turn " + object + " into a Component");
-                }
-
-                String s = (String)object;
-                this.append(Component.literal(s));
-            }
-        }
-
+    public MutableComponent append(Component p_130942_) {
+        this.siblings.add(p_130942_);
         return this;
     }
 
@@ -114,13 +_,12 @@
     public boolean equals(Object p_237209_) {
         if (this == p_237209_) {
             return true;
-        } else if (!(p_237209_ instanceof MutableComponent)) {
-            return false;
         } else {
-            MutableComponent mutablecomponent = (MutableComponent)p_237209_;
-            return this.contents.equals(mutablecomponent.contents)
-                && this.style.equals(mutablecomponent.style)
-                && this.siblings.equals(mutablecomponent.siblings);
+            return !(p_237209_ instanceof MutableComponent mutablecomponent)
+                ? false
+                : this.contents.equals(mutablecomponent.contents)
+                    && this.style.equals(mutablecomponent.style)
+                    && this.siblings.equals(mutablecomponent.siblings);
         }
     }
 
