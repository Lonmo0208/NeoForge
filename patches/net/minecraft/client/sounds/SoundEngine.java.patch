--- a/net/minecraft/client/sounds/SoundEngine.java
+++ b/net/minecraft/client/sounds/SoundEngine.java
@@ -82,6 +_,7 @@
         this.soundManager = soundManager;
         this.options = options;
         this.soundBuffers = new SoundBufferLibrary(resourceProvider);
+        net.neoforged.fml.ModLoader.postEvent(new net.neoforged.neoforge.client.event.sound.SoundEngineLoadEvent(this));
     }
 
     public void reload() {
@@ -99,6 +_,7 @@
 
         this.destroy();
         this.loadLibrary();
+        net.neoforged.fml.ModLoader.postEvent(new net.neoforged.neoforge.client.event.sound.SoundEngineLoadEvent(this));
     }
 
     private synchronized void loadLibrary() {
@@ -338,7 +_,9 @@
     public SoundEngine.PlayResult play(SoundInstance instance) {
         if (!this.loaded) {
             return SoundEngine.PlayResult.NOT_STARTED;
-        } else if (!instance.canPlaySound()) {
+        }
+        instance = net.neoforged.neoforge.client.ClientHooks.playSound(this, instance);
+        if (instance == null || !instance.canPlaySound()) {
             return SoundEngine.PlayResult.NOT_STARTED;
         } else {
             WeighedSoundEvents weighedsoundevents = instance.resolve(this.soundManager);
@@ -420,15 +_,18 @@
                         channel.setSelfPosition(vec3);
                         channel.setRelative(flag);
                     });
+                    final SoundInstance soundinstance = instance;
                     if (!flag1) {
                         this.soundBuffers.getCompleteBuffer(sound.getPath()).thenAccept(soundBuffer -> channelaccess$channelhandle.execute(channel -> {
                             channel.attachStaticBuffer(soundBuffer);
                             channel.play();
+                            net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.sound.PlaySoundSourceEvent(this, soundinstance, channel));
                         }));
                     } else {
-                        this.soundBuffers.getStream(sound.getPath(), flag3).thenAccept(stream -> channelaccess$channelhandle.execute(channel -> {
+                        soundinstance.getStream(this.soundBuffers, sound, flag3).thenAccept(stream -> channelaccess$channelhandle.execute(channel -> {
                             channel.attachBufferStream(stream);
                             channel.play();
+                            net.neoforged.neoforge.common.NeoForge.EVENT_BUS.post(new net.neoforged.neoforge.client.event.sound.PlayStreamingSourceEvent(this, soundinstance, channel));
                         }));
                     }
 
