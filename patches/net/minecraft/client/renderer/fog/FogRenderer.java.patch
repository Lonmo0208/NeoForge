--- a/net/minecraft/client/renderer/fog/FogRenderer.java
+++ b/net/minecraft/client/renderer/fog/FogRenderer.java
@@ -151,7 +_,7 @@
                 f2 = Mth.lerp(f6, f2, f2 * f7);
             }
 
-            return new Vector4f(f5, f1, f2, 1.0F);
+            return net.neoforged.neoforge.client.ClientHooks.getFogColor(camera, partialTicks, level, renderDistance, darkenWorldAmount, f5, f1, f2);
         }
     }
 
@@ -167,9 +_,11 @@
         Entity entity = camera.entity();
         FogData fogdata = new FogData();
 
+        FogEnvironment chosenEnvironment = null;
         for (FogEnvironment fogenvironment : FOG_ENVIRONMENTS) {
             if (fogenvironment.isApplicable(fogtype, entity)) {
                 fogenvironment.setupFog(fogdata, camera, level, f1, deltaTracker);
+                chosenEnvironment = fogenvironment;
                 break;
             }
         }
@@ -177,6 +_,8 @@
         float f2 = Mth.clamp(f1 / 10.0F, 4.0F, 64.0F);
         fogdata.renderDistanceStart = f1 - f2;
         fogdata.renderDistanceEnd = f1;
+
+        net.neoforged.neoforge.client.ClientHooks.onSetupFog(chosenEnvironment, fogtype, camera, f, renderDistanceInChunks, fogdata);
 
         try (GpuBuffer.MappedView gpubuffer$mappedview = RenderSystem.getDevice()
                 .createCommandEncoder()
