--- a/net/minecraft/client/renderer/block/ModelBlockRenderer.java
+++ b/net/minecraft/client/renderer/block/ModelBlockRenderer.java
@@ -38,6 +_,7 @@
         this.blockColors = blockColors;
     }
 
+    @Deprecated // Neo: Buffer lookup parameter
     public void tesselateBlock(
         BlockAndTintGetter level,
         List<BlockModelPart> parts,
@@ -48,15 +_,33 @@
         boolean cull,
         int overlayCoords
     ) {
+        tesselateBlock(level, parts, blockState, pos, poseStack, type -> builder, cull, overlayCoords);
+    }
+
+    public void tesselateBlock(
+        BlockAndTintGetter level,
+        List<BlockModelPart> parts,
+        BlockState blockState,
+        BlockPos pos,
+        PoseStack poseStack,
+        java.util.function.Function<net.minecraft.client.renderer.chunk.ChunkSectionLayer, VertexConsumer> bufferLookup,
+        boolean cull,
+        int overlayCoords
+    ) {
         if (!parts.isEmpty()) {
-            boolean flag = Minecraft.useAmbientOcclusion() && blockState.getLightEmission() == 0 && parts.getFirst().useAmbientOcclusion();
+            boolean perPartAO = net.neoforged.neoforge.client.config.NeoForgeClientConfig.INSTANCE.handleAmbientOcclusionPerPart.getAsBoolean();
+            boolean flag = Minecraft.useAmbientOcclusion() && (perPartAO || switch(parts.getFirst().ambientOcclusion()) {
+                case TRUE -> true;
+                case DEFAULT -> blockState.getLightEmission(level, pos) == 0;
+                case FALSE -> false;
+            });
             poseStack.translate(blockState.getOffset(pos));
 
             try {
                 if (flag) {
-                    this.tesselateWithAO(level, parts, blockState, pos, poseStack, builder, cull, overlayCoords);
+                    this.tesselateWithAO(level, parts, blockState, pos, poseStack, bufferLookup, cull, overlayCoords);
                 } else {
-                    this.tesselateWithoutAO(level, parts, blockState, pos, poseStack, builder, cull, overlayCoords);
+                    this.tesselateWithoutAO(level, parts, blockState, pos, poseStack, bufferLookup, cull, overlayCoords);
                 }
             } catch (Throwable throwable) {
                 CrashReport crashreport = CrashReport.forThrowable(throwable, "Tesselating block model");
@@ -68,6 +_,8 @@
         }
     }
 
+    /** @deprecated Neo: use {@link #shouldRenderFace(BlockAndTintGetter, BlockPos, BlockState, boolean, Direction, BlockPos)} instead */
+    @Deprecated
     private static boolean shouldRenderFace(BlockAndTintGetter level, BlockState state, boolean cullEnabled, Direction direction, BlockPos neighborPos) {
         if (!cullEnabled) {
             return true;
@@ -77,6 +_,16 @@
         }
     }
 
+    protected static boolean shouldRenderFace(BlockAndTintGetter level, BlockPos pos, BlockState state, boolean cullEnabled, Direction direction, BlockPos neighborPos) {
+        if (!cullEnabled) {
+            return true;
+        } else {
+            BlockState blockstate = level.getBlockState(neighborPos);
+            return Block.shouldRenderFace(level, pos, state, blockstate, direction);
+        }
+    }
+
+    @Deprecated // Neo: Buffer lookup parameter
     public void tesselateWithAO(
         BlockAndTintGetter level,
         List<BlockModelPart> parts,
@@ -87,11 +_,38 @@
         boolean cull,
         int overlayCoords
     ) {
-        ModelBlockRenderer.AmbientOcclusionRenderStorage modelblockrenderer$ambientocclusionrenderstorage = new ModelBlockRenderer.AmbientOcclusionRenderStorage();
+        this.tesselateWithAO(level, parts, state, pos, poseStack, type -> builder, cull, overlayCoords);
+    }
+
+    public void tesselateWithAO(
+        BlockAndTintGetter level,
+        List<BlockModelPart> parts,
+        BlockState state,
+        BlockPos pos,
+        PoseStack poseStack,
+        java.util.function.Function<net.minecraft.client.renderer.chunk.ChunkSectionLayer, VertexConsumer> bufferLookup,
+        boolean cull,
+        int overlayCoords
+    ) {
+        // Neo: Inject enhanced AO pipeline
+        ModelBlockRenderer.AmbientOcclusionRenderStorage modelblockrenderer$ambientocclusionrenderstorage = net.neoforged.neoforge.client.model.ao.EnhancedAoRenderStorage.newInstance();
+        boolean perPartAO = net.neoforged.neoforge.client.config.NeoForgeClientConfig.INSTANCE.handleAmbientOcclusionPerPart.getAsBoolean();
+        int lightEmission = -1;
         int i = 0;
         int j = 0;
 
         for (BlockModelPart blockmodelpart : parts) {
+            VertexConsumer builder = bufferLookup.apply(blockmodelpart.getRenderType(state));
+            boolean ao = !perPartAO || switch (blockmodelpart.ambientOcclusion()) {
+                case TRUE -> true;
+                case DEFAULT -> {
+                    if (lightEmission == -1) {
+                        lightEmission = state.getLightEmission(level, pos);
+                    }
+                    yield lightEmission == 0;
+                }
+                case FALSE -> false;
+            };
             for (Direction direction : DIRECTIONS) {
                 int k = 1 << direction.ordinal();
                 boolean flag = (i & k) == 1;
@@ -102,6 +_,7 @@
                         if (!flag) {
                             flag1 = shouldRenderFace(
                                 level,
+                                pos,
                                 state,
                                 cull,
                                 direction,
@@ -114,6 +_,12 @@
                         }
 
                         if (flag1) {
+                            if (!ao) {
+                                int light = modelblockrenderer$ambientocclusionrenderstorage.cache.getLightColor(state, level, modelblockrenderer$ambientocclusionrenderstorage.scratchPos.setWithOffset(pos, direction));
+                                this.renderModelFaceFlat(
+                                        level, state, pos, light, overlayCoords, false, poseStack, builder, list, modelblockrenderer$ambientocclusionrenderstorage
+                                );
+                            } else
                             this.renderModelFaceAO(
                                 level, state, pos, poseStack, builder, list, modelblockrenderer$ambientocclusionrenderstorage, overlayCoords
                             );
@@ -124,6 +_,11 @@
 
             List<BakedQuad> list1 = blockmodelpart.getQuads(null);
             if (!list1.isEmpty()) {
+                if (!ao) {
+                    this.renderModelFaceFlat(
+                            level, state, pos, -1, overlayCoords, true, poseStack, builder, list1, modelblockrenderer$ambientocclusionrenderstorage
+                    );
+                } else
                 this.renderModelFaceAO(
                     level, state, pos, poseStack, builder, list1, modelblockrenderer$ambientocclusionrenderstorage, overlayCoords
                 );
@@ -131,6 +_,7 @@
         }
     }
 
+    @Deprecated // Neo: Buffer lookup parameter
     public void tesselateWithoutAO(
         BlockAndTintGetter level,
         List<BlockModelPart> parts,
@@ -141,11 +_,25 @@
         boolean cull,
         int overlayCoords
     ) {
+        this.tesselateWithoutAO(level, parts, state, pos, poseStack, type -> builder, cull, overlayCoords);
+    }
+
+    public void tesselateWithoutAO(
+        BlockAndTintGetter level,
+        List<BlockModelPart> parts,
+        BlockState state,
+        BlockPos pos,
+        PoseStack poseStack,
+        java.util.function.Function<net.minecraft.client.renderer.chunk.ChunkSectionLayer, VertexConsumer> bufferLookup,
+        boolean cull,
+        int overlayCoords
+    ) {
         ModelBlockRenderer.CommonRenderStorage modelblockrenderer$commonrenderstorage = new ModelBlockRenderer.CommonRenderStorage();
         int i = 0;
         int j = 0;
 
         for (BlockModelPart blockmodelpart : parts) {
+            VertexConsumer builder = bufferLookup.apply(blockmodelpart.getRenderType(state));
             for (Direction direction : DIRECTIONS) {
                 int k = 1 << direction.ordinal();
                 boolean flag = (i & k) == 1;
@@ -155,7 +_,7 @@
                     if (!list.isEmpty()) {
                         BlockPos blockpos = modelblockrenderer$commonrenderstorage.scratchPos.setWithOffset(pos, direction);
                         if (!flag) {
-                            flag1 = shouldRenderFace(level, state, cull, direction, blockpos);
+                            flag1 = shouldRenderFace(level, pos, state, cull, direction, blockpos);
                             i |= k;
                             if (flag1) {
                                 j |= k;
@@ -192,7 +_,12 @@
         int overlayCoords
     ) {
         for (BakedQuad bakedquad : quads) {
+            if (!bakedquad.hasAmbientOcclusion()) {
+                renderModelQuadFlat(level, state, pos, -1, overlayCoords, true, poseStack, builder, bakedquad, storage);
+                continue;
+            }
             calculateShape(level, state, pos, bakedquad, storage);
+            storage.captureQuad(bakedquad);
             storage.calculate(level, state, pos, bakedquad.direction(), bakedquad.shade());
             this.putQuadData(level, state, pos, builder, poseStack.last(), bakedquad, storage, overlayCoords);
         }
@@ -304,17 +_,30 @@
         ModelBlockRenderer.CommonRenderStorage shapeState
     ) {
         for (BakedQuad bakedquad : quads) {
+            renderModelQuadFlat(level, state, pos, lightColor, overlayCoords, checkLight, poseStack, builder, bakedquad, shapeState);
+        }
+    }
+
+    private void renderModelQuadFlat(
+        BlockAndTintGetter level,
+        BlockState state,
+        BlockPos pos,
+        int lightColor,
+        int overlayCoords,
+        boolean checkLight,
+        PoseStack poseStack,
+        VertexConsumer builder,
+        BakedQuad bakedquad,
+        ModelBlockRenderer.CommonRenderStorage shapeState
+    ) {
+        {
             if (checkLight) {
                 calculateShape(level, state, pos, bakedquad, shapeState);
                 BlockPos blockpos = (BlockPos)(shapeState.faceCubic ? shapeState.scratchPos.setWithOffset(pos, bakedquad.direction()) : pos);
                 lightColor = shapeState.cache.getLightColor(state, level, blockpos);
             }
 
-            float f = level.getShade(bakedquad.direction(), bakedquad.shade());
-            shapeState.brightness[0] = f;
-            shapeState.brightness[1] = f;
-            shapeState.brightness[2] = f;
-            shapeState.brightness[3] = f;
+            net.neoforged.neoforge.client.model.ao.EnhancedAoRenderStorage.applyFlatQuadBrightness(level, bakedquad, shapeState);
             shapeState.lightmap[0] = lightColor;
             shapeState.lightmap[1] = lightColor;
             shapeState.lightmap[2] = lightColor;
@@ -323,6 +_,7 @@
         }
     }
 
+    @Deprecated // Neo: Level, position, and buffer source parameters
     public static void renderModel(
         PoseStack.Pose pose,
         VertexConsumer builder,
@@ -333,7 +_,24 @@
         int lightCoords,
         int overlayCoords
     ) {
-        for (BlockModelPart blockmodelpart : model.collectParts(RandomSource.create(42L))) {
+        renderModel(pose, type -> builder, model, r, g, b, lightCoords, overlayCoords, net.minecraft.world.level.EmptyBlockAndTintGetter.INSTANCE, BlockPos.ZERO, net.minecraft.world.level.block.Blocks.AIR.defaultBlockState());
+    }
+
+    public static void renderModel(
+        PoseStack.Pose pose,
+        net.minecraft.client.renderer.MultiBufferSource bufferSource,
+        BlockStateModel model,
+        float r,
+        float g,
+        float b,
+        int lightCoords,
+        int overlayCoords,
+        net.minecraft.world.level.BlockAndTintGetter level,
+        BlockPos pos,
+        BlockState state
+    ) {
+        for (BlockModelPart blockmodelpart : model.collectParts(level, pos, state, RandomSource.create(42L))) {
+            VertexConsumer builder = bufferSource.getBuffer(net.neoforged.neoforge.client.RenderTypeHelper.getEntityRenderType(blockmodelpart.getRenderType(state)));
             for (Direction direction : DIRECTIONS) {
                 renderQuadList(pose, builder, r, g, b, blockmodelpart.getQuads(direction), lightCoords, overlayCoords);
             }
@@ -694,6 +_,9 @@
 
         public AmbientOcclusionRenderStorage() {
         }
+
+        // Neo: Call this before calling calculate if the render storage is an EnhancedAoRenderStorage!
+        public void captureQuad(BakedQuad quad) {}
 
         public void calculate(BlockAndTintGetter level, BlockState state, BlockPos centerPosition, Direction direction, boolean shade) {
             BlockPos blockpos = this.faceCubic ? centerPosition.relative(direction) : centerPosition;
