--- a/net/minecraft/client/renderer/FogRenderer.java
+++ b/net/minecraft/client/renderer/FogRenderer.java
@@ -31,7 +_,7 @@
 public class FogRenderer {
     private static final int WATER_FOG_DISTANCE = 96;
     private static final List<FogRenderer.MobEffectFogFunction> MOB_EFFECT_FOG = Lists.newArrayList(
-        new FogRenderer.BlindnessFogFunction(), new FogRenderer.DarknessFogFunction()
+            new FogRenderer.BlindnessFogFunction(), new FogRenderer.DarknessFogFunction()
     );
     public static final float BIOME_FOG_TRANSITION_TIME = 5000.0F;
     private static float fogRed;
@@ -41,35 +_,42 @@
     private static int previousBiomeFog = -1;
     private static long biomeChangedTime = -1L;
 
-    public static void setupColor(Camera p_109019_, float p_109020_, ClientLevel p_109021_, int p_109022_, float p_109023_) {
-        FogType fogtype = p_109019_.getFluidInCamera();
-        Entity entity = p_109019_.getEntity();
+    public static void setupColor(Camera camera, float partialTicks, ClientLevel level, int renderDistance, float darkness) {
+        FogType fogtype = camera.getFluidInCamera();
+        Entity entity = camera.getEntity();
+        Vec3 cameraPos = camera.getPosition();
+        long currentTime = Util.getMillis();
+
         if (fogtype == FogType.WATER) {
-            long i = Util.getMillis();
-            int j = p_109021_.getBiome(BlockPos.containing(p_109019_.getPosition())).value().getWaterFogColor();
+            BlockPos pos = BlockPos.containing(cameraPos);
+            int newBiomeFog = level.getBiome(pos).value().getWaterFogColor();
+
             if (biomeChangedTime < 0L) {
-                targetBiomeFog = j;
-                previousBiomeFog = j;
-                biomeChangedTime = i;
+                targetBiomeFog = newBiomeFog;
+                previousBiomeFog = newBiomeFog;
+                biomeChangedTime = currentTime;
             }
 
-            int k = targetBiomeFog >> 16 & 0xFF;
-            int l = targetBiomeFog >> 8 & 0xFF;
-            int i1 = targetBiomeFog & 0xFF;
-            int j1 = previousBiomeFog >> 16 & 0xFF;
-            int k1 = previousBiomeFog >> 8 & 0xFF;
-            int l1 = previousBiomeFog & 0xFF;
-            float f = Mth.clamp((float)(i - biomeChangedTime) / 5000.0F, 0.0F, 1.0F);
-            float f1 = Mth.lerp(f, (float)j1, (float)k);
-            float f2 = Mth.lerp(f, (float)k1, (float)l);
-            float f3 = Mth.lerp(f, (float)l1, (float)i1);
-            fogRed = f1 / 255.0F;
-            fogGreen = f2 / 255.0F;
-            fogBlue = f3 / 255.0F;
-            if (targetBiomeFog != j) {
-                targetBiomeFog = j;
-                previousBiomeFog = Mth.floor(f1) << 16 | Mth.floor(f2) << 8 | Mth.floor(f3);
-                biomeChangedTime = i;
+            int prevR = previousBiomeFog >> 16 & 0xFF;
+            int prevG = previousBiomeFog >> 8 & 0xFF;
+            int prevB = previousBiomeFog & 0xFF;
+            int targetR = targetBiomeFog >> 16 & 0xFF;
+            int targetG = targetBiomeFog >> 8 & 0xFF;
+            int targetB = targetBiomeFog & 0xFF;
+
+            float interpFactor = Mth.clamp((float)(currentTime - biomeChangedTime) / BIOME_FOG_TRANSITION_TIME, 0.0F, 1.0F);
+            float r = Mth.lerp(interpFactor, prevR, targetR) / 255.0F;
+            float g = Mth.lerp(interpFactor, prevG, targetG) / 255.0F;
+            float b = Mth.lerp(interpFactor, prevB, targetB) / 255.0F;
+
+            fogRed = r;
+            fogGreen = g;
+            fogBlue = b;
+
+            if (targetBiomeFog != newBiomeFog) {
+                targetBiomeFog = newBiomeFog;
+                previousBiomeFog = (Mth.floor(r * 255) << 16 | (Mth.floor(g * 255) << 8 | Mth.floor(b * 255)));
+                biomeChangedTime = currentTime;
             }
         } else if (fogtype == FogType.LAVA) {
             fogRed = 0.6F;
@@ -83,119 +_,106 @@
             biomeChangedTime = -1L;
             RenderSystem.clearColor(fogRed, fogGreen, fogBlue, 0.0F);
         } else {
-            float f4 = 0.25F + 0.75F * (float)p_109022_ / 32.0F;
-            f4 = 1.0F - (float)Math.pow((double)f4, 0.25);
-            Vec3 vec3 = p_109021_.getSkyColor(p_109019_.getPosition(), p_109020_);
-            float f6 = (float)vec3.x;
-            float f8 = (float)vec3.y;
-            float f10 = (float)vec3.z;
-            float f11 = Mth.clamp(Mth.cos(p_109021_.getTimeOfDay(p_109020_) * (float) (Math.PI * 2)) * 2.0F + 0.5F, 0.0F, 1.0F);
-            BiomeManager biomemanager = p_109021_.getBiomeManager();
-            Vec3 vec31 = p_109019_.getPosition().subtract(2.0, 2.0, 2.0).scale(0.25);
-            Vec3 vec32 = CubicSampler.gaussianSampleVec3(
-                vec31,
-                (p_109033_, p_109034_, p_109035_) -> p_109021_.effects()
-                        .getBrightnessDependentFogColor(
-                            Vec3.fromRGB24(biomemanager.getNoiseBiomeAtQuart(p_109033_, p_109034_, p_109035_).value().getFogColor()), f11
-                        )
+            float renderFactor = 0.25F + 0.75F * renderDistance / 32.0F;
+            float skyFactor = 1.0F - (float)Math.pow(renderFactor, 0.25);
+            Vec3 skyColor = level.getSkyColor(cameraPos, partialTicks);
+            float skyR = (float)skyColor.x;
+            float skyG = (float)skyColor.y;
+            float skyB = (float)skyColor.z;
+
+            float timeFactor = Mth.clamp(Mth.cos(level.getTimeOfDay(partialTicks) * ((float)Math.PI * 2)) * 2.0F + 0.5F, 0.0F, 1.0F);
+
+            BiomeManager biomeMgr = level.getBiomeManager();
+            Vec3 samplePos = cameraPos.subtract(2.0, 2.0, 2.0).scale(0.25);
+            Vec3 fogColor = CubicSampler.gaussianSampleVec3(
+                    samplePos,
+                    (x, y, z) -> level.effects().getBrightnessDependentFogColor(
+                            Vec3.fromRGB24(biomeMgr.getNoiseBiomeAtQuart(x, y, z).value().getFogColor()),
+                            timeFactor
+                    )
             );
-            fogRed = (float)vec32.x();
-            fogGreen = (float)vec32.y();
-            fogBlue = (float)vec32.z();
-            if (p_109022_ >= 4) {
-                float f12 = Mth.sin(p_109021_.getSunAngle(p_109020_)) > 0.0F ? -1.0F : 1.0F;
-                Vector3f vector3f = new Vector3f(f12, 0.0F, 0.0F);
-                float f16 = p_109019_.getLookVector().dot(vector3f);
-                if (f16 < 0.0F) {
-                    f16 = 0.0F;
-                }
-
-                if (f16 > 0.0F) {
-                    float[] afloat = p_109021_.effects().getSunriseColor(p_109021_.getTimeOfDay(p_109020_), p_109020_);
-                    if (afloat != null) {
-                        f16 *= afloat[3];
-                        fogRed = fogRed * (1.0F - f16) + afloat[0] * f16;
-                        fogGreen = fogGreen * (1.0F - f16) + afloat[1] * f16;
-                        fogBlue = fogBlue * (1.0F - f16) + afloat[2] * f16;
+
+            fogRed = (float)fogColor.x();
+            fogGreen = (float)fogColor.y();
+            fogBlue = (float)fogColor.z();
+
+            if (renderDistance >= 4) {
+                float sunDot = camera.getLookVector().dot(new Vector3f(Mth.sin(level.getSunAngle(partialTicks)) > 0.0F ? -1.0F : 1.0F, 0.0F, 0.0F));
+                if (sunDot > 0.0F) {
+                    float[] sunrise = level.effects().getSunriseColor(level.getTimeOfDay(partialTicks), partialTicks);
+                    if (sunrise != null) {
+                        float sunFactor = sunDot * sunrise[3];
+                        fogRed = fogRed * (1.0F - sunFactor) + sunrise[0] * sunFactor;
+                        fogGreen = fogGreen * (1.0F - sunFactor) + sunrise[1] * sunFactor;
+                        fogBlue = fogBlue * (1.0F - sunFactor) + sunrise[2] * sunFactor;
                     }
                 }
             }
 
-            fogRed = fogRed + (f6 - fogRed) * f4;
-            fogGreen = fogGreen + (f8 - fogGreen) * f4;
-            fogBlue = fogBlue + (f10 - fogBlue) * f4;
-            float f13 = p_109021_.getRainLevel(p_109020_);
-            if (f13 > 0.0F) {
-                float f14 = 1.0F - f13 * 0.5F;
-                float f17 = 1.0F - f13 * 0.4F;
-                fogRed *= f14;
-                fogGreen *= f14;
-                fogBlue *= f17;
+            fogRed += (skyR - fogRed) * skyFactor;
+            fogGreen += (skyG - fogGreen) * skyFactor;
+            fogBlue += (skyB - fogBlue) * skyFactor;
+
+            float rainLevel = level.getRainLevel(partialTicks);
+            if (rainLevel > 0.0F) {
+                float rainFactorR = 1.0F - rainLevel * 0.5F;
+                float rainFactorB = 1.0F - rainLevel * 0.4F;
+                fogRed *= rainFactorR;
+                fogGreen *= rainFactorR;
+                fogBlue *= rainFactorB;
             }
 
-            float f15 = p_109021_.getThunderLevel(p_109020_);
-            if (f15 > 0.0F) {
-                float f18 = 1.0F - f15 * 0.5F;
-                fogRed *= f18;
-                fogGreen *= f18;
-                fogBlue *= f18;
+            float thunderLevel = level.getThunderLevel(partialTicks);
+            if (thunderLevel > 0.0F) {
+                float thunderFactor = 1.0F - thunderLevel * 0.5F;
+                fogRed *= thunderFactor;
+                fogGreen *= thunderFactor;
+                fogBlue *= thunderFactor;
             }
 
             biomeChangedTime = -1L;
         }
 
-        float f5 = ((float)p_109019_.getPosition().y - (float)p_109021_.getMinBuildHeight()) * p_109021_.getLevelData().getClearColorScale();
-        FogRenderer.MobEffectFogFunction fogrenderer$mobeffectfogfunction = getPriorityFogFunction(entity, p_109020_);
-        if (fogrenderer$mobeffectfogfunction != null) {
-            LivingEntity livingentity = (LivingEntity)entity;
-            f5 = fogrenderer$mobeffectfogfunction.getModifiedVoidDarkness(
-                livingentity, livingentity.getEffect(fogrenderer$mobeffectfogfunction.getMobEffect()), f5, p_109020_
-            );
-        }
-
-        if (f5 < 1.0F && fogtype != FogType.LAVA && fogtype != FogType.POWDER_SNOW) {
-            if (f5 < 0.0F) {
-                f5 = 0.0F;
-            }
-
-            f5 *= f5;
-            fogRed *= f5;
-            fogGreen *= f5;
-            fogBlue *= f5;
-        }
-
-        if (p_109023_ > 0.0F) {
-            fogRed = fogRed * (1.0F - p_109023_) + fogRed * 0.7F * p_109023_;
-            fogGreen = fogGreen * (1.0F - p_109023_) + fogGreen * 0.6F * p_109023_;
-            fogBlue = fogBlue * (1.0F - p_109023_) + fogBlue * 0.6F * p_109023_;
-        }
-
-        float f7;
-        if (fogtype == FogType.WATER) {
-            if (entity instanceof LocalPlayer) {
-                f7 = ((LocalPlayer)entity).getWaterVision();
-            } else {
-                f7 = 1.0F;
-            }
-        } else {
-            label86: {
-                if (entity instanceof LivingEntity livingentity1
-                    && livingentity1.hasEffect(MobEffects.NIGHT_VISION)
-                    && !livingentity1.hasEffect(MobEffects.DARKNESS)) {
-                    f7 = GameRenderer.getNightVisionScale(livingentity1, p_109020_);
-                    break label86;
-                }
-
-                f7 = 0.0F;
-            }
-        }
-
-        if (fogRed != 0.0F && fogGreen != 0.0F && fogBlue != 0.0F) {
-            float f9 = Math.min(1.0F / fogRed, Math.min(1.0F / fogGreen, 1.0F / fogBlue));
-            fogRed = fogRed * (1.0F - f7) + fogRed * f9 * f7;
-            fogGreen = fogGreen * (1.0F - f7) + fogGreen * f9 * f7;
-            fogBlue = fogBlue * (1.0F - f7) + fogBlue * f9 * f7;
-        }
+        float heightFactor = ((float)cameraPos.y - level.getMinBuildHeight()) * level.getLevelData().getClearColorScale();
+        FogRenderer.MobEffectFogFunction effectFunc = getPriorityFogFunction(entity, darkness);
+        if (effectFunc != null && entity instanceof LivingEntity living) {
+            heightFactor = effectFunc.getModifiedVoidDarkness(living, living.getEffect(effectFunc.getMobEffect()), heightFactor, darkness);
+        }
+
+        if (heightFactor < 1.0F && fogtype != FogType.LAVA && fogtype != FogType.POWDER_SNOW) {
+            if (heightFactor < 0.0F) heightFactor = 0.0F;
+            heightFactor *= heightFactor; // 平方衰减更平滑
+            fogRed *= heightFactor;
+            fogGreen *= heightFactor;
+            fogBlue *= heightFactor;
+        }
+
+        if (darkness > 0.0F) {
+            fogRed = fogRed * (1.0F - darkness) + fogRed * 0.7F * darkness;
+            fogGreen = fogGreen * (1.0F - darkness) + fogGreen * 0.6F * darkness;
+            fogBlue = fogBlue * (1.0F - darkness) + fogBlue * 0.6F * darkness;
+        }
+
+        float visionScale = 0.0F;
+        if (fogtype == FogType.WATER && entity instanceof LocalPlayer player) {
+            visionScale = player.getWaterVision();
+        } else if (entity instanceof LivingEntity living) {
+            if (living.hasEffect(MobEffects.NIGHT_VISION) && !living.hasEffect(MobEffects.DARKNESS)) {
+                visionScale = GameRenderer.getNightVisionScale(living, darkness);
+            }
+        }
+
+        if (visionScale > 0.0F && fogRed != 0.0F && fogGreen != 0.0F && fogBlue != 0.0F) {
+            float maxChannel = Math.min(1.0F / fogRed, Math.min(1.0F / fogGreen, 1.0F / fogBlue));
+            fogRed = Mth.lerp(visionScale, fogRed, fogRed * maxChannel);
+            fogGreen = Mth.lerp(visionScale, fogGreen, fogGreen * maxChannel);
+            fogBlue = Mth.lerp(visionScale, fogBlue, fogBlue * maxChannel);
+        }
+
+        Vector3f fogColor = net.neoforged.neoforge.client.ClientHooks.getFogColor(camera, partialTicks, level, renderDistance, darkness, fogRed, fogGreen, fogBlue);
+        fogRed = fogColor.x();
+        fogGreen = fogColor.y();
+        fogBlue = fogColor.z();
 
         RenderSystem.clearColor(fogRed, fogGreen, fogBlue, 0.0F);
     }
@@ -205,74 +_,73 @@
     }
 
     @Nullable
-    private static FogRenderer.MobEffectFogFunction getPriorityFogFunction(Entity p_234166_, float p_234167_) {
-        return p_234166_ instanceof LivingEntity livingentity
-            ? MOB_EFFECT_FOG.stream().filter(p_234171_ -> p_234171_.isEnabled(livingentity, p_234167_)).findFirst().orElse(null)
-            : null;
+    private static FogRenderer.MobEffectFogFunction getPriorityFogFunction(Entity entity, float partialTicks) {
+        if (!(entity instanceof LivingEntity living)) return null;
+        for (FogRenderer.MobEffectFogFunction func : MOB_EFFECT_FOG) {
+            if (func.isEnabled(living, partialTicks)) return func;
+        }
+        return null;
     }
 
-    public static void setupFog(Camera p_234173_, FogRenderer.FogMode p_234174_, float p_234175_, boolean p_234176_, float p_234177_) {
-        FogType fogtype = p_234173_.getFluidInCamera();
-        Entity entity = p_234173_.getEntity();
-        FogRenderer.FogData fogrenderer$fogdata = new FogRenderer.FogData(p_234174_);
-        FogRenderer.MobEffectFogFunction fogrenderer$mobeffectfogfunction = getPriorityFogFunction(entity, p_234177_);
+    public static void setupFog(Camera camera, FogRenderer.FogMode fogMode, float renderDistance, boolean thickFog, float partialTicks) {
+        FogType fogtype = camera.getFluidInCamera();
+        Entity entity = camera.getEntity();
+        FogRenderer.FogData fogData = new FogRenderer.FogData(fogMode);
+        FogRenderer.MobEffectFogFunction effectFunc = getPriorityFogFunction(entity, partialTicks);
+
         if (fogtype == FogType.LAVA) {
             if (entity.isSpectator()) {
-                fogrenderer$fogdata.start = -8.0F;
-                fogrenderer$fogdata.end = p_234175_ * 0.5F;
-            } else if (entity instanceof LivingEntity && ((LivingEntity)entity).hasEffect(MobEffects.FIRE_RESISTANCE)) {
-                fogrenderer$fogdata.start = 0.0F;
-                fogrenderer$fogdata.end = 5.0F;
+                fogData.start = -8.0F;
+                fogData.end = renderDistance * 0.5F;
+            } else if (entity instanceof LivingEntity living && living.hasEffect(MobEffects.FIRE_RESISTANCE)) {
+                fogData.start = 0.0F;
+                fogData.end = 5.0F;
             } else {
-                fogrenderer$fogdata.start = 0.25F;
-                fogrenderer$fogdata.end = 1.0F;
+                fogData.start = 0.25F;
+                fogData.end = 1.0F;
             }
         } else if (fogtype == FogType.POWDER_SNOW) {
             if (entity.isSpectator()) {
-                fogrenderer$fogdata.start = -8.0F;
-                fogrenderer$fogdata.end = p_234175_ * 0.5F;
+                fogData.start = -8.0F;
+                fogData.end = renderDistance * 0.5F;
             } else {
-                fogrenderer$fogdata.start = 0.0F;
-                fogrenderer$fogdata.end = 2.0F;
-            }
-        } else if (fogrenderer$mobeffectfogfunction != null) {
-            LivingEntity livingentity = (LivingEntity)entity;
-            MobEffectInstance mobeffectinstance = livingentity.getEffect(fogrenderer$mobeffectfogfunction.getMobEffect());
-            if (mobeffectinstance != null) {
-                fogrenderer$mobeffectfogfunction.setupFog(fogrenderer$fogdata, livingentity, mobeffectinstance, p_234175_, p_234177_);
-            }
+                fogData.start = 0.0F;
+                fogData.end = 2.0F;
+            }
+        } else if (effectFunc != null && entity instanceof LivingEntity living) {
+            MobEffectInstance effect = living.getEffect(effectFunc.getMobEffect());
+            if (effect != null) effectFunc.setupFog(fogData, living, effect, renderDistance, partialTicks);
         } else if (fogtype == FogType.WATER) {
-            fogrenderer$fogdata.start = -8.0F;
-            fogrenderer$fogdata.end = 96.0F;
-            if (entity instanceof LocalPlayer localplayer) {
-                fogrenderer$fogdata.end = fogrenderer$fogdata.end * Math.max(0.25F, localplayer.getWaterVision());
-                Holder<Biome> holder = localplayer.level().getBiome(localplayer.blockPosition());
-                if (holder.is(BiomeTags.HAS_CLOSER_WATER_FOG)) {
-                    fogrenderer$fogdata.end *= 0.85F;
+            fogData.start = -8.0F;
+            fogData.end = WATER_FOG_DISTANCE;
+            if (entity instanceof LocalPlayer player) {
+                fogData.end *= Math.max(0.25F, player.getWaterVision());
+                if (player.level().getBiome(player.blockPosition()).is(BiomeTags.HAS_CLOSER_WATER_FOG)) {
+                    fogData.end *= 0.85F;
                 }
             }
-
-            if (fogrenderer$fogdata.end > p_234175_) {
-                fogrenderer$fogdata.end = p_234175_;
-                fogrenderer$fogdata.shape = FogShape.CYLINDER;
+            if (fogData.end > renderDistance) {
+                fogData.end = renderDistance;
+                fogData.shape = FogShape.CYLINDER;
             }
-        } else if (p_234176_) {
-            fogrenderer$fogdata.start = p_234175_ * 0.05F;
-            fogrenderer$fogdata.end = Math.min(p_234175_, 192.0F) * 0.5F;
-        } else if (p_234174_ == FogRenderer.FogMode.FOG_SKY) {
-            fogrenderer$fogdata.start = 0.0F;
-            fogrenderer$fogdata.end = p_234175_;
-            fogrenderer$fogdata.shape = FogShape.CYLINDER;
+        } else if (thickFog) {
+            fogData.start = renderDistance * 0.05F;
+            fogData.end = Math.min(renderDistance, 192.0F) * 0.5F;
+        } else if (fogMode == FogRenderer.FogMode.FOG_SKY) {
+            fogData.start = 0.0F;
+            fogData.end = renderDistance;
+            fogData.shape = FogShape.CYLINDER;
         } else {
-            float f = Mth.clamp(p_234175_ / 10.0F, 4.0F, 64.0F);
-            fogrenderer$fogdata.start = p_234175_ - f;
-            fogrenderer$fogdata.end = p_234175_;
-            fogrenderer$fogdata.shape = FogShape.CYLINDER;
+            float fogRange = Mth.clamp(renderDistance / 10.0F, 4.0F, 64.0F);
+            fogData.start = renderDistance - fogRange;
+            fogData.end = renderDistance;
+            fogData.shape = FogShape.CYLINDER;
         }
 
-        RenderSystem.setShaderFogStart(fogrenderer$fogdata.start);
-        RenderSystem.setShaderFogEnd(fogrenderer$fogdata.end);
-        RenderSystem.setShaderFogShape(fogrenderer$fogdata.shape);
+        RenderSystem.setShaderFogStart(fogData.start);
+        RenderSystem.setShaderFogEnd(fogData.end);
+        RenderSystem.setShaderFogShape(fogData.shape);
+        net.neoforged.neoforge.client.ClientHooks.onFogRender(fogMode, fogtype, camera, partialTicks, renderDistance, fogData.start, fogData.end, fogData.shape);
     }
 
     public static void levelFogColor() {
@@ -287,14 +_,16 @@
         }
 
         @Override
-        public void setupFog(FogRenderer.FogData p_234181_, LivingEntity p_234182_, MobEffectInstance p_234183_, float p_234184_, float p_234185_) {
-            float f = p_234183_.isInfiniteDuration() ? 5.0F : Mth.lerp(Math.min(1.0F, (float)p_234183_.getDuration() / 20.0F), p_234184_, 5.0F);
-            if (p_234181_.mode == FogRenderer.FogMode.FOG_SKY) {
-                p_234181_.start = 0.0F;
-                p_234181_.end = f * 0.8F;
+        public void setupFog(FogRenderer.FogData fogData, LivingEntity entity, MobEffectInstance effect, float renderDistance, float partialTicks) {
+            float distance = effect.isInfiniteDuration() ? 5.0F :
+                    Mth.lerp(Math.min(1.0F, effect.getDuration() / 20.0F), renderDistance, 5.0F);
+
+            if (fogData.mode == FogRenderer.FogMode.FOG_SKY) {
+                fogData.start = 0.0F;
+                fogData.end = distance * 0.8F;
             } else {
-                p_234181_.start = f * 0.25F;
-                p_234181_.end = f;
+                fogData.start = distance * 0.25F;
+                fogData.end = distance;
             }
         }
     }
@@ -307,15 +_,15 @@
         }
 
         @Override
-        public void setupFog(FogRenderer.FogData p_234194_, LivingEntity p_234195_, MobEffectInstance p_234196_, float p_234197_, float p_234198_) {
-            float f = Mth.lerp(p_234196_.getBlendFactor(p_234195_, p_234198_), p_234197_, 15.0F);
-            p_234194_.start = p_234194_.mode == FogRenderer.FogMode.FOG_SKY ? 0.0F : f * 0.75F;
-            p_234194_.end = f;
+        public void setupFog(FogRenderer.FogData fogData, LivingEntity entity, MobEffectInstance effect, float renderDistance, float partialTicks) {
+            float distance = Mth.lerp(effect.getBlendFactor(entity, partialTicks), renderDistance, 15.0F);
+            fogData.start = (fogData.mode == FogRenderer.FogMode.FOG_SKY) ? 0.0F : distance * 0.75F;
+            fogData.end = distance;
         }
 
         @Override
-        public float getModifiedVoidDarkness(LivingEntity p_234189_, MobEffectInstance p_234190_, float p_234191_, float p_234192_) {
-            return 1.0F - p_234190_.getBlendFactor(p_234189_, p_234192_);
+        public float getModifiedVoidDarkness(LivingEntity entity, MobEffectInstance effect, float heightFactor, float partialTicks) {
+            return 1.0F - effect.getBlendFactor(entity, partialTicks);
         }
     }
 
@@ -326,8 +_,8 @@
         public float end;
         public FogShape shape = FogShape.SPHERE;
 
-        public FogData(FogRenderer.FogMode p_234204_) {
-            this.mode = p_234204_;
+        public FogData(FogRenderer.FogMode mode) {
+            this.mode = mode;
         }
     }
 
@@ -340,24 +_,17 @@
     @OnlyIn(Dist.CLIENT)
     interface MobEffectFogFunction {
         Holder<MobEffect> getMobEffect();
-
-        void setupFog(FogRenderer.FogData p_234212_, LivingEntity p_234213_, MobEffectInstance p_234214_, float p_234215_, float p_234216_);
-
-        default boolean isEnabled(LivingEntity p_234206_, float p_234207_) {
-            return p_234206_.hasEffect(this.getMobEffect());
+        void setupFog(FogRenderer.FogData fogData, LivingEntity entity, MobEffectInstance effect, float renderDistance, float partialTicks);
+
+        default boolean isEnabled(LivingEntity entity, float partialTicks) {
+            return entity.hasEffect(getMobEffect());
         }
 
-        default float getModifiedVoidDarkness(LivingEntity p_234208_, MobEffectInstance p_234209_, float p_234210_, float p_234211_) {
-            MobEffectInstance mobeffectinstance = p_234208_.getEffect(this.getMobEffect());
-            if (mobeffectinstance != null) {
-                if (mobeffectinstance.endsWithin(19)) {
-                    p_234210_ = 1.0F - (float)mobeffectinstance.getDuration() / 20.0F;
-                } else {
-                    p_234210_ = 0.0F;
-                }
+        default float getModifiedVoidDarkness(LivingEntity entity, MobEffectInstance effect, float heightFactor, float partialTicks) {
+            if (effect != null && effect.endsWithin(19)) {
+                return 1.0F - effect.getDuration() / 20.0F;
             }
-
-            return p_234210_;
+            return heightFactor;
         }
     }
 }
