--- a/net/minecraft/client/renderer/texture/atlas/SpriteResourceLoader.java
+++ b/net/minecraft/client/renderer/texture/atlas/SpriteResourceLoader.java
@@ -27,7 +_,7 @@
     Logger LOGGER = LogUtils.getLogger();
 
     static SpriteResourceLoader create(Set<MetadataSectionType<?>> additionalMetadataSections) {
-        return (spriteLocation, resource) -> {
+        return (spriteLocation, resource, constructor) -> {
             Optional<AnimationMetadataSection> optional;
             Optional<TextureMetadataSection> optional1;
             List<MetadataSectionType.WithValue<?>> list;
@@ -68,9 +_,14 @@
                 framesize = new FrameSize(nativeimage.getWidth(), nativeimage.getHeight());
             }
 
-            return new SpriteContents(spriteLocation, framesize, nativeimage, optional, list, optional1);
+            return constructor.create(spriteLocation, framesize, nativeimage, optional, list, optional1);
         };
     }
 
-    @Nullable SpriteContents loadSprite(Identifier spriteLocation, Resource resource);
+    default SpriteContents loadSprite(Identifier p_295581_, Resource resource) {
+        return loadSprite(p_295581_, resource, SpriteContents::new);
+    }
+
+    @Nullable
+    SpriteContents loadSprite(Identifier id, Resource resource, net.neoforged.neoforge.client.textures.SpriteContentsConstructor constructor);
 }
