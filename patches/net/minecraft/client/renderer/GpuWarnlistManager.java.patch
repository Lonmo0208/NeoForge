--- a/net/minecraft/client/renderer/GpuWarnlistManager.java
+++ b/net/minecraft/client/renderer/GpuWarnlistManager.java
@@ -85,15 +_,15 @@
 
     @Nullable
     public String getAllWarnings() {
-        StringBuilder stringbuilder = new StringBuilder();
+        StringBuilder stringbuilder = new StringBuilder(128);
         this.warnings.forEach((p_109235_, p_109236_) -> stringbuilder.append(p_109235_).append(": ").append(p_109236_));
         return stringbuilder.length() == 0 ? null : stringbuilder.toString();
     }
 
     protected GpuWarnlistManager.Preparations prepare(ResourceManager p_109220_, ProfilerFiller p_109221_) {
-        List<Pattern> list = Lists.newArrayList();
-        List<Pattern> list1 = Lists.newArrayList();
-        List<Pattern> list2 = Lists.newArrayList();
+        List<Pattern> list = new java.util.ArrayList<>();
+        List<Pattern> list1 = new java.util.ArrayList<>();
+        List<Pattern> list2 = new java.util.ArrayList<>();
         p_109221_.startTick();
         JsonObject jsonobject = parseJson(p_109220_, p_109221_);
         if (jsonobject != null) {
@@ -113,7 +_,9 @@
     }
 
     private static void compilePatterns(JsonArray p_109223_, List<Pattern> p_109224_) {
-        p_109223_.forEach(p_109239_ -> p_109224_.add(Pattern.compile(p_109239_.getAsString(), 2)));
+        for (JsonElement element : p_109223_) {
+            p_109224_.add(Pattern.compile(element.getAsString(), 2));
+        }
     }
 
     @Nullable
@@ -143,35 +_,49 @@
             this.vendorPatterns = p_109263_;
         }
 
-        private static String matchAny(List<Pattern> p_109273_, String p_109274_) {
-            List<String> list = Lists.newArrayList();
-
-            for (Pattern pattern : p_109273_) {
-                Matcher matcher = pattern.matcher(p_109274_);
-
+        private static String matchAny(List<Pattern> patterns, String input) {
+            if (patterns.isEmpty()) {
+                return "";
+            }
+            StringBuilder matches = new StringBuilder(64);
+            boolean first = true;
+            for (Pattern pattern : patterns) {
+                Matcher matcher = pattern.matcher(input);
                 while (matcher.find()) {
-                    list.add(matcher.group());
+                    if (!first) {
+                        matches.append(", ");
+                    }
+                    matches.append(matcher.group());
+                    first = false;
                 }
             }
-
-            return String.join(", ", list);
+            return matches.toString();
         }
 
         ImmutableMap<String, String> apply() {
             Builder<String, String> builder = new Builder<>();
-            String s = matchAny(this.rendererPatterns, GlUtil.getRenderer());
-            if (!s.isEmpty()) {
-                builder.put("renderer", s);
-            }
-
-            String s1 = matchAny(this.versionPatterns, GlUtil.getOpenGLVersion());
-            if (!s1.isEmpty()) {
-                builder.put("version", s1);
-            }
-
-            String s2 = matchAny(this.vendorPatterns, GlUtil.getVendor());
-            if (!s2.isEmpty()) {
-                builder.put("vendor", s2);
+            String renderer = GlUtil.getRenderer();
+            if (!renderer.isEmpty()) {
+                String s = matchAny(this.rendererPatterns, renderer);
+                if (!s.isEmpty()) {
+                    builder.put("renderer", s);
+                }
+            }
+
+            String version = GlUtil.getOpenGLVersion();
+            if (!version.isEmpty()) {
+                String s1 = matchAny(this.versionPatterns, version);
+                if (!s1.isEmpty()) {
+                    builder.put("version", s1);
+                }
+            }
+
+            String vendor = GlUtil.getVendor();
+            if (!vendor.isEmpty()) {
+                String s2 = matchAny(this.vendorPatterns, vendor);
+                if (!s2.isEmpty()) {
+                    builder.put("vendor", s2);
+                }
             }
 
             return builder.build();
