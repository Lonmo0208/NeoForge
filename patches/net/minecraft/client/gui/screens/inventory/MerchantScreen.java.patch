--- a/net/minecraft/client/gui/screens/inventory/MerchantScreen.java
+++ b/net/minecraft/client/gui/screens/inventory/MerchantScreen.java
@@ -58,7 +_,7 @@
     private boolean isDragging;
 
     public MerchantScreen(MerchantMenu menu, Inventory inventory, Component title) {
-        super(menu, inventory, title, 276, 78);
+        super(menu, inventory, title, 276, 166);
         this.inventoryLabelX = 107;
     }
 
@@ -234,7 +_,11 @@
             graphics.itemDecorations(this.font, costA, sellItem1X, decorHeight);
         } else {
             graphics.itemDecorations(this.font, baseCostA, sellItem1X, decorHeight, baseCostA.getCount() == 1 ? "1" : null);
-            graphics.itemDecorations(this.font, costA, sellItem1X + 14, decorHeight, costA.getCount() == 1 ? "1" : null);
+            // Neo: Fixes MCForge#8806: forced item decorations (e.g., ItemStack#isBarVisible() returns true) were rendered for discount count text
+            // Code for count rendering taken from GuiGraphicsExtractor#renderItemCount
+            String count = costA.getCount() == 1 ? "1" : String.valueOf(costA.getCount());
+            graphics.text(font, count, sellItem1X + 14 + 19 - 2 - font.width(count), decorHeight + 6 + 3, 0xFFFFFFFF, true);
+
             graphics.blitSprite(RenderPipelines.GUI_TEXTURED, DISCOUNT_STRIKETHRUOGH_SPRITE, sellItem1X + 7, decorHeight + 12, 9, 2);
         }
     }
