--- a/com/mojang/blaze3d/opengl/DirectStateAccess.java
+++ b/com/mojang/blaze3d/opengl/DirectStateAccess.java
@@ -42,7 +_,11 @@
 
     abstract int createFrameBufferObject();
 
-    abstract void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, int bindSlot);
+    abstract void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, @GpuBuffer.Usage int bindSlot, boolean useStencil);
+
+    public void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, @GpuBuffer.Usage int bindSlot) {
+        bindFrameBufferTextures(fbo, color0, depth, mipLevel, bindSlot, false);
+    }
 
     abstract void blitFrameBuffers(
         int source,
@@ -112,9 +_,14 @@
         }
 
         @Override
-        public void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, @GpuBuffer.Usage int bindSlot) {
+        public void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, @GpuBuffer.Usage int bindSlot, boolean useStencil) {
             ARBDirectStateAccess.glNamedFramebufferTexture(fbo, 36064, color0, mipLevel);
             ARBDirectStateAccess.glNamedFramebufferTexture(fbo, 36096, depth, mipLevel);
+            if (useStencil) {
+                ARBDirectStateAccess.glNamedFramebufferTexture(fbo, org.lwjgl.opengl.GL32C.GL_STENCIL_ATTACHMENT, depth, mipLevel);
+            } else {
+                ARBDirectStateAccess.glNamedFramebufferTexture(fbo, org.lwjgl.opengl.GL32C.GL_STENCIL_ATTACHMENT, 0, 0);
+            }
             if (bindSlot != 0) {
                 GlStateManager._glBindFramebuffer(bindSlot, fbo);
             }
@@ -248,12 +_,17 @@
         }
 
         @Override
-        public void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, int bindSlot) {
+        public void bindFrameBufferTextures(int fbo, int color0, int depth, int mipLevel, int bindSlot, boolean useStencil) {
             int i = bindSlot == 0 ? '\u8ca9' : bindSlot;
             int j = GlStateManager.getFrameBuffer(i);
             GlStateManager._glBindFramebuffer(i, fbo);
             GlStateManager._glFramebufferTexture2D(i, 36064, 3553, color0, mipLevel);
             GlStateManager._glFramebufferTexture2D(i, 36096, 3553, depth, mipLevel);
+            if (useStencil) {
+                GlStateManager._glFramebufferTexture2D(i, org.lwjgl.opengl.GL32C.GL_STENCIL_ATTACHMENT, 3553, depth, mipLevel);
+            } else {
+                GlStateManager._glFramebufferTexture2D(i, org.lwjgl.opengl.GL32C.GL_STENCIL_ATTACHMENT, 3553, 0, 0);
+            }
             if (bindSlot == 0) {
                 GlStateManager._glBindFramebuffer(i, j);
             }
